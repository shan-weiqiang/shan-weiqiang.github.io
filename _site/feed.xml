<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="http://localhost:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2024-05-05T17:30:12+08:00</updated><id>http://localhost:4000/feed.xml</id><title type="html">Shan Weiqiang’s blog</title><subtitle></subtitle><entry><title type="html">ara::com API 解读[Part 1]</title><link href="http://localhost:4000/2024/05/05/ara-com-API-%E8%A7%A3%E8%AF%BB.html" rel="alternate" type="text/html" title="ara::com API 解读[Part 1]" /><published>2024-05-05T13:22:46+08:00</published><updated>2024-05-05T13:22:46+08:00</updated><id>http://localhost:4000/2024/05/05/ara::com-API-%E8%A7%A3%E8%AF%BB</id><content type="html" xml:base="http://localhost:4000/2024/05/05/ara-com-API-%E8%A7%A3%E8%AF%BB.html"><![CDATA[<p><a href="https://www.autosar.org/fileadmin/standards/R23-11/AP/AUTOSAR_AP_EXP_ARAComAPI.pdf">ara::com API</a>是理解AUTOSAR AP中面向服务架构的理解入口。无论对于底层BSW开发还是对于模型和代码引擎的开发，还是对于上层应用开发，都是需要理解的。可以说ara::com API是模型、BSW开发、模型/代码引擎开发的三者交汇点。理解它非常重要。本文是第一部分。</p>

<ul id="markdown-toc">
  <li><a href="#3-introduction" id="markdown-toc-3-introduction">3 Introduction</a>    <ul>
      <li><a href="#31-approach" id="markdown-toc-31-approach">3.1 Approach</a></li>
      <li><a href="#32-api-design-visions-and-guidelines" id="markdown-toc-32-api-design-visions-and-guidelines">3.2 API Design Visions and Guidelines</a></li>
    </ul>
  </li>
  <li><a href="#4-fundamentals" id="markdown-toc-4-fundamentals">4 Fundamentals</a>    <ul>
      <li><a href="#41-proxyskeleton-architecture" id="markdown-toc-41-proxyskeleton-architecture">4.1 Proxy/Skeleton Architecture</a></li>
      <li><a href="#43-aracom-event-and-trigger-based-communication" id="markdown-toc-43-aracom-event-and-trigger-based-communication">4.3 ara::com Event and Trigger based communication</a></li>
      <li><a href="#44-aracom-method-based-communication" id="markdown-toc-44-aracom-method-based-communication">4.4 ara::com Method based communication</a></li>
      <li><a href="#48-service-connection-approach" id="markdown-toc-48-service-connection-approach">4.8 Service Connection Approach</a>        <ul>
          <li><a href="#481-instance-identifiers-and-instance-specifiers" id="markdown-toc-481-instance-identifiers-and-instance-specifiers">4.8.1 Instance Identifiers and Instance Specifiers</a></li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<h1 id="3-introduction">3 Introduction</h1>

<h2 id="31-approach">3.1 Approach</h2>

<p>标准列举了为什么要设计一个新的API的原因，这些原因也是ComAPI的核心特点：</p>

<blockquote>
  <p>We need a Communication Management, which is NOT bound to a concrete
network communication protocol. It has to support the SOME/IP protocol but
there has to be flexibility to exchange that.</p>
</blockquote>

<hr />
<p><strong>NOTE</strong></p>

<p>ComAPI应当支持多种底层通信协议。这个特征在其他API中也有支持，比如ROS API。目前AUTOSAR AP规范中提供了两种协议绑定支持：SOME/IP，DDS。在AUTOSAR的模型中，主线是按照SOME/IP规范设计的，DDS的绑定模型多数是为了跟SOME/IP的模型对称。相比SOME/IP，DDS协议对底层传输层的封装更彻底，用户无需指定具体的传输层端口号，协议等也可以按照标准的xml文件指定。服务发现方面，DDS只需要Domain ID和Topic名称即可在VLAN中通信，而SOME/IP需要通过具体的组播发现和EventGroup等概念来进行服务发现，详细可见<a href="https://shan-weiqiang.github.io/2024/04/08/SOME-IP-SD-%E8%A7%A3%E8%AF%BB.html">SOME/IP Service Discovery解读</a>, <a href="https://shan-weiqiang.github.io/2024/04/19/SOME-IP-%E8%A7%A3%E8%AF%BB.html">SOME/IP 解读</a>。总之，SOME/IP协议是AUTOSAR官方的协议，AUTOSAR的模型系统是基于SOME/IP设计的，在对其他协议进行模型设计时，也需要首先熟知SOME/IP，否则AUTOSAR Manifest中模型的来源就会变得莫名其妙。</p>

<hr />

<blockquote>
  <p>The AUTOSAR service model, which defines services as a collection of provided
methods, events and fields shall be supported naturally/straight forward.</p>
</blockquote>

<hr />
<p><strong>NOTE</strong></p>

<p>如果底层通信的驱动协议，如DDS没有这些概念，则需要在绑定的时候通过中间层支持。</p>

<hr />

<blockquote>
  <p>The API shall support an event-driven and a polling model to get access to communicated data equally well. The latter one is typically needed by real-time applications to avoid unnecessary context switches, while the former one is much
more convenient for applications without real-time requirements</p>
</blockquote>

<hr />
<p><strong>NOTE</strong></p>

<p>Polling意味着在上层应用的线程中直接获取数据，不会阻塞线程，也不会发生线程的上下文切换；Polling模式需要底层有消息缓冲池用于存放到达的消息队列。事件驱动模式从操作系统层面大致有两种执行方式，第一种是同步执行，即由通信线程在收到消息后直接调用应用层的回调，这种方式会阻塞通信的消息接收；另一种是异步执行，通过使用任务执行线程池来处理到达的消息以及回调，这种方式不会阻塞消息的接收，但是会有线程的上下文切换。</p>

<hr />

<blockquote>
  <p>Possibility for seamless integration of end-to-end protection to fulfill ASIL requirements.</p>
</blockquote>

<hr />
<p><strong>NOTE</strong></p>

<p>E2E是功能安全必须的。发送端对数据的保护发生在序列化后，接收端解保护发生在反序列化前，与底层的通信协议并没有直接的关系，任何协议都可以实现E2E。</p>

<hr />

<blockquote>
  <p>Support for static (preconfigured) and dynamic (runtime) selection of service instances to communicate with.</p>
</blockquote>

<hr />
<p><strong>NOTE</strong></p>

<p>注意这里不是动态和静态的服务发现，而是动态和静态的选择服务实例；这通常是从Proxy的角度出发来说的，AUTOSAR AP的ComAPI接口中可以在FindService的时候指定具体的通信服务实例，也可以发现所有的匹配成功的服务实例，Proxy可以动态的在运行时选择与哪一个服务实例进行通信。</p>

<p>注意：一个Proxy类的实例只能与一个服务实例进行通信；如果一个Proxy需要同时与多个服务实例通信，则必须实例化多个Proxy类的实例；Proxy类的FindService方法通常是<code class="language-plaintext highlighter-rouge">static</code>的。</p>

<hr />

<h2 id="32-api-design-visions-and-guidelines">3.2 API Design Visions and Guidelines</h2>

<blockquote>
  <p>Consequently, ara::com does not provide any kind of component model or framework, which would take care of things like component life cycle, management of program flow or simply setting up ara::com API objects according to the formal component description of the respective application.</p>

  <p>All this could be easily built on top of the basic ara::com API and needs not be
standardized to support typical collaboration models.</p>
</blockquote>

<hr />
<p><strong>NOTE</strong></p>

<p>ComAPI仅仅提供了Proxy/Skeleton类的标准方法接口，不提供类的封装以及更上层SWC的封装方法</p>

<hr />

<h1 id="4-fundamentals">4 Fundamentals</h1>

<h2 id="41-proxyskeleton-architecture">4.1 Proxy/Skeleton Architecture</h2>

<p><img src="/assets/images/proxy_skeleton.png" alt="Alt text" /></p>

<hr />
<p><strong>NOTE</strong></p>

<p>Service Interface Definition: 服务接口的定义。这个是通过AUTOSAR的模型定义的。AUTOSAR的模型系统本质上是一种接口描述语言（IDL)，可以参照DDS的idl文件，Google Protobuf的proto文件，或者ROS2的.msg文件。与以上这些IDL不同，AUTOSAR的模型系统覆盖面更广泛，不仅仅是数据类型的定义，它包括了整个软件平台的硬件和软件。AUTOSAR AP可以理解为对整个平台软件，硬件的建模。AUTOSAR AP的代码引擎将AUTOSAR AP的模型编译成两部分内容：<code class="language-plaintext highlighter-rouge">code</code>和<code class="language-plaintext highlighter-rouge">manifest</code>，即代码和配置清单。模型是一种更高级层级抽象的语言，代码引擎就是这种语言的编译器。所以在这个图中，使用了<code class="language-plaintext highlighter-rouge">generated from</code>字样，其实是代码引擎的功能。</p>

<p>Middleware Transport Layer： 这一层指的是通信的驱动协议，可以是上面提到的SOME/IP， DDS等</p>

<p>Service Proxy/Skeleton: 服务的消费方和提供方。在Proxy端，类的Findservice方法是<code class="language-plaintext highlighter-rouge">static</code>的，可以通过这个类的静态方法找到多个Skeleton的服务实例，从类的层面Proxy与Skeleton是N-N的通信模式，即一个Proxy类可以与多个Skeleton类通信，一个Skeleton类也可以与多个Proxy类通信，这些都是同时的。但是，Proxy与Skeleton不同的地方在于类的实例，在Proxy类通过静态方法找到对应的Skeleton服务实例后，需要动态的创建Proxy的类的实例，而这个Proxy类的实例只能与唯一的一个Skeleton的服务实例通信；而Skeleton端则不同，一个Skeleton的类的实例就可以实现同时与多个Proxy的服务实例通信，所以Skeleton类不需要静态方法。在实际应用中，如果一个Proxy类需要从多个Skeleton服务实例接收服务，则需要注意应用消费函数方法的可重入性.</p>

<p>Service Consumer Implementation： Proxy一侧的应用层实现，例如在Polling模式中调用GetNewSample获取数据；在事件驱动模式中，注册的新消息到达后的回调实现等；这些实现通过标准的ComAPI接口与应用层进行连接，其实现本身并不是ComAPI的一部分。</p>

<p>Service Implementation：服务的应用层实现，例如应用层调用Send接口发送数据；method中注册应用层的方法实现回调等；这些实现通过标准的ComAPI接口与应用层连接，其实现本身并不是ComAPI的一部分。</p>

<p>从以上可以看出，一个Client或者Service应用，需要三部分的代码才能实现：</p>

<ul>
  <li>应用层的实现代码</li>
  <li>ComAPI标准接口代码</li>
  <li>底层通信驱动协议代码</li>
</ul>

<p>其实，在具体的协议绑定过程中还需要协议绑定的适配代码。</p>

<hr />

<h2 id="43-aracom-event-and-trigger-based-communication">4.3 ara::com Event and Trigger based communication</h2>

<p><img src="/assets/images/event.png" alt="Alt text" /></p>

<hr />
<p><strong>NOTE</strong></p>

<p>这个图中的每个角色需要解释一下：</p>

<p>Client/Server application: 服务的消费和提供方的应用代码；这部分是服务和消费的具体实现，代码中主要是调用ComAPI的标准接口，注册回调等。例如Send，Subscribe，method的处理函数回调，消息的时间触发回调，获取消息GetNewSample等都是在应用代码中调用的。</p>

<p>Proxy/Skeleton： 这部分是标准的ComAPI接口类，提供了AUTOSAR定义的标准接口API</p>

<p>Communication Management Middleware： 这部分是比较特殊的代码，可以认为是ComAPI与底层驱动协议的适配代码(adaptor)，它的作用在于基于底层不同的通信协议，来实现上层的ComAPI，从而使应用代码进需要使用ComAPI，而无需关系底层的通信驱动协议。这部分代码通常是跟底层通信协议一对一的，例如SOME/IP和DDS，它们分别有自己的适配代码。</p>

<p>Network Binding: 底层的通信协议绑定，例如SOME/IP， DDS。注意不要跟OSI的传输层混淆。</p>

<p>图中还有一点需要注意，在收到消息后，是在Communication Management Middleware缓存收到的新消息的，这一点要求来自SOME/IP，因为SOME/IP协议本身没有历史消息等缓存机制；但是在DDS协议中，在Network Binding这一层就可以实现消息的缓存，可以省去一次拷贝操作。</p>

<p>在GetNewSamples接口中，传入的是一个用户回调，Communication Management Middleware这一层需要按照顺序逐个对每一个消息调用该回调。这是因为不是每一个到达的消息都会调用RecieveHandler，这个循环处理所有的消息是必须的。</p>

<p>Triggers和events的使用方式是一样的，只不过triggers不会发送数据，类似一个信号</p>

<hr />

<h2 id="44-aracom-method-based-communication">4.4 ara::com Method based communication</h2>

<p><img src="/assets/images/method.png" alt="Alt text" /></p>

<hr />
<p><strong>NOTE</strong></p>

<p>method的实现基于<code class="language-plaintext highlighter-rouge">promise</code>/<code class="language-plaintext highlighter-rouge">future</code>组合，功能是实现method请求端和处理端的异步。注意在请求端和处理端分别有一组<code class="language-plaintext highlighter-rouge">promise</code>/<code class="language-plaintext highlighter-rouge">future</code>组合。</p>

<p>在请求端，method API接口返回一个<code class="language-plaintext highlighter-rouge">future</code>，其对应的<code class="language-plaintext highlighter-rouge">promise</code>应当是在Communication Management Middleware中，所以对于图中在Network Binding中返回一个<code class="language-plaintext highlighter-rouge">future</code>，笔者持保留意见。大部分通信协议，例如SOME/IP，DDS本身并不原生支持method，所以返回一个<code class="language-plaintext highlighter-rouge">future</code>是不大可能的。<code class="language-plaintext highlighter-rouge">future</code>通常是在Communication Management Middleware中间适配层的对象，除此之外中间适配层还要完成session管理。</p>

<p>在method处理端，可以有三种模式：</p>

<ul>
  <li>kEvent： 并发的事件驱动模式；具体实现上一般是个线程池</li>
  <li>kEventSingleThread: 事件驱动，单线程</li>
  <li>Polling： 在用户的Polling方法中处理并发送</li>
</ul>

<p>这里需要额外说明Fire&amp;Forget的特殊情况，特别是当返回值是<code class="language-plaintext highlighter-rouge">void</code>的情形：</p>

<ul>
  <li>如果method是Fire&amp;Forget，则method请求端无需等待处理端的返回；注意即便返回值类型是<code class="language-plaintext highlighter-rouge">void</code>，也需要等待处理端的返回</li>
  <li>如果不是Fire&amp;Forget，则即便返回值类型是<code class="language-plaintext highlighter-rouge">void</code>，请求端也需要等待处理端返回的信号</li>
</ul>

<p>与event相比，method还有一个额外的特征需要管理，就是session管理。在SOME/IP协议中，这是通过Client ID来完成的，详见<a href="https://shan-weiqiang.github.io/2024/04/19/SOME-IP-%E8%A7%A3%E8%AF%BB.html#4124-request-id-32-bit">Request ID</a>. 在其他的协议绑定，例如DDS中，需要在通信适配层来对session进行管理。</p>

<hr />

<h2 id="48-service-connection-approach">4.8 Service Connection Approach</h2>

<h3 id="481-instance-identifiers-and-instance-specifiers">4.8.1 Instance Identifiers and Instance Specifiers</h3>

<blockquote>
  <p>Instance identifiers are used within ara::com, on client/proxy side, when a specific
instance of a service shall be searched for or — at the server/skeleton side — when a
specific instance of a service is created.</p>
</blockquote>

<hr />
<p><strong>NOTE</strong></p>

<p>Instance identifier 表示一个服务的Instance，一个服务的Instance有一个全局唯一的Instance ID。只有Skeleton服务实例才有，才确定一个Instance ID。Proxy端是没有服务ID的，但需要指定一个其需要发现的服务ID或者发现所有服务ID。</p>

<hr />

<blockquote>
  <p>If the unambiguousness is ensured, the integrator/deployer can assign a dedicated
technical binding with its specific instance IDs to those “instance specifier” via a “manifest file”, which is specifically used for a distinct instantiation/execution of the executable.</p>

  <p>This explicitly allows, to start the same executable N times, each time with a different
manifest, which maps the same ara::core::InstanceSpecifier differently</p>
</blockquote>

<hr />
<p><strong>NOTE</strong></p>

<p>Instance identifier和Instance specifier这两个概念必须结合AUTOSAR的模型来理解，否则很难get到其中的概念。而标准中对这两个概念的解释，也佐证了为什么AUTOSAR能把一个概念越解释越难理解。</p>

<p>要想完整的理解这两个概念，必须阅读<a href="https://www.autosar.org/fileadmin/standards/R23-11/AP/AUTOSAR_AP_TPS_ManifestSpecification.pdf">Specification of Manifest</a>中<code class="language-plaintext highlighter-rouge">ServiceInstanceToPortPrototypeMapping</code>这个模型以及与其相关的模型。</p>

<p>在AUTOSAR AP的模型中一个PortPrototype是一个Service Interface在应用软件（Software Component）中的实例化，而一个Service Instance是一个Service Interface在驱动协议中的实例化。每一个应用软件的每一个PortPrototype都有一个全局唯一的模型路径，例如<code class="language-plaintext highlighter-rouge">/a/b/c</code>，而这个路径就是所谓的Instance Specifier;每一个Service Instance在模型中也有自己的全局唯一的路径，例如<code class="language-plaintext highlighter-rouge">/d/e/f</code>，这个路径就是所谓的Instance Identifier（也唯一的对应一个Instance ID）。</p>

<p><code class="language-plaintext highlighter-rouge">ServiceInstanceToPortPrototypeMapping</code>的作用就是将Instance Identifier映射到Instance Specifier。且每一个Instance Specifier可以映射多个Service Identifier（multi-binding）。 这在现实中的意义是：一个SWC的一个PPort口可以通过ComAPI在底层多种通信协议下进行数据发送；一个SWC的RPort口可以同时从多个驱动协议的通道获取服务（不常见）。</p>

<p>同一个Instance Specifier在不同的模型配置中，都可以映射不同的Instance Identifier（但是Service Interface必须对应一致），这就是文中所说的<em>This explicitly allows, to start the same executable N times, each time with a different manifest, which maps the same ara::core::InstanceSpecifier differently</em>的含义。</p>

<p>Instance Identifier和Instance Specifier都是<em>部署</em>阶段的模型元素，代码引擎会根据不同的部署模型生成不同的部署配置文件清单（例如json），在配置清单中会详细的包含Instance Specifier与Instance Identifier的对应关系。这就是文中从Instance Specifier解析Instance Identifier的信息来源：解析配置清单，获取对应信息。</p>

<hr />]]></content><author><name></name></author><category term="AUTOSAR" /><summary type="html"><![CDATA[ara::com API是理解AUTOSAR AP中面向服务架构的理解入口。无论对于底层BSW开发还是对于模型和代码引擎的开发，还是对于上层应用开发，都是需要理解的。可以说ara::com API是模型、BSW开发、模型/代码引擎开发的三者交汇点。理解它非常重要。本文是第一部分。]]></summary></entry><entry><title type="html">condition_variable fully explained</title><link href="http://localhost:4000/2024/04/27/condition_variable-fully-explained.html" rel="alternate" type="text/html" title="condition_variable fully explained" /><published>2024-04-27T19:22:46+08:00</published><updated>2024-04-27T19:22:46+08:00</updated><id>http://localhost:4000/2024/04/27/condition_variable%20-fully-explained</id><content type="html" xml:base="http://localhost:4000/2024/04/27/condition_variable-fully-explained.html"><![CDATA[<p>This post tries to enumerate the pitfalls that might happen in the use of <code class="language-plaintext highlighter-rouge">condition_variable</code>. First I will explain scenarios in the use of <code class="language-plaintext highlighter-rouge">condition_variable</code>; then give a summary about tips of using it; At the end of the post, a live example is given to demonstrate what have been talked about in this post. Hope this will help someone to spent one less miniute debugging <code class="language-plaintext highlighter-rouge">condition_variable</code>.</p>

<ul id="markdown-toc">
  <li><a href="#thread-flow" id="markdown-toc-thread-flow">Thread flow</a>    <ul>
      <li><a href="#waiting-thread" id="markdown-toc-waiting-thread">Waiting thread</a></li>
      <li><a href="#notifying-thread" id="markdown-toc-notifying-thread">Notifying thread</a></li>
      <li><a href="#the-matrix" id="markdown-toc-the-matrix">The matrix</a>        <ul>
          <li><a href="#t1-scenario" id="markdown-toc-t1-scenario">T1 scenario</a></li>
          <li><a href="#t2-scenario" id="markdown-toc-t2-scenario">T2 scenario</a></li>
          <li><a href="#t3-scenario" id="markdown-toc-t3-scenario">T3 scenario</a></li>
        </ul>
      </li>
    </ul>
  </li>
  <li><a href="#golden-rules" id="markdown-toc-golden-rules">Golden rules</a></li>
  <li><a href="#real-world-example" id="markdown-toc-real-world-example">Real world example</a></li>
</ul>

<h1 id="thread-flow">Thread flow</h1>

<p>To understand <code class="language-plaintext highlighter-rouge">condition_variable</code>, we assume following premises:</p>

<ul>
  <li>There is only one notifying thread</li>
  <li>There is only one waiting thread</li>
  <li>The notify_one/notify_all will only be called once</li>
  <li>The condition is atomic bool</li>
</ul>

<p>You might think these premises are too much and what’s the point of analysing it with so many premises. As it turns out, even with these premises, the situation is so complicated that it deserves efforts to understand it. Besides, after we are clear about this, we can understand all situations that are not bounded by those premises.</p>

<h2 id="waiting-thread">Waiting thread</h2>

<p><code class="language-plaintext highlighter-rouge">condition_variable</code> is implemented on <code class="language-plaintext highlighter-rouge">futex</code> in Linux, I will not go deeper about it at here, it’s too big a topic, more info can be found <a href="https://man7.org/linux/man-pages/man2/futex.2.html">here</a>. All we need to know in this blog is following:</p>

<ul>
  <li>Iniside the <code class="language-plaintext highlighter-rouge">wait</code> function call, the mutex associated with it will be unlocked before blocking</li>
  <li>The unlock of the mutex and the changing to block state is done atomically</li>
  <li>After wake up of the blocking thread, the thread will first tries to lock the associated mutex, before return</li>
</ul>

<p>Following is the diagram showing the state of the waiting thread:</p>

<p><img src="/assets/images/waiting.png" alt="Alt text" /></p>

<p>In this diagram:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Var</code> is the atomic <em>condition</em></li>
  <li><code class="language-plaintext highlighter-rouge">mtx</code> is the mutex</li>
  <li>The grey area is area that are protected by the <code class="language-plaintext highlighter-rouge">mtx</code></li>
  <li>The <code class="language-plaintext highlighter-rouge">CP1</code> to <code class="language-plaintext highlighter-rouge">CP8</code> are checkpoints to indicate where the waiting thread might possibly be when the notifying thread calls <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code></li>
  <li>The <code class="language-plaintext highlighter-rouge">CPb</code> means block state, it is done atomically with <code class="language-plaintext highlighter-rouge">CP4</code>, here I draw it specifically to demonstrate this point</li>
  <li>The dashed rectangular is to indicate the <code class="language-plaintext highlighter-rouge">wait(..)</code> function call. Note that inside this function call, there is unlock and lock of the <code class="language-plaintext highlighter-rouge">mtx</code></li>
  <li>Also I draw a loop, which is a typical use of the waiting thread, which might be is inside a infinite <code class="language-plaintext highlighter-rouge">while</code> loop</li>
</ul>

<h2 id="notifying-thread">Notifying thread</h2>

<p>The notifying thread is the thread that changes the <em>condition</em> and calls <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code>. We consider all the three possible way of calling these methods:</p>

<ul>
  <li>Without holding the <code class="language-plaintext highlighter-rouge">mtx</code> at all</li>
  <li>Holding the lock while changing the <em>condition</em> only</li>
  <li>Holding the lock while changing the <em>condition</em> and calling <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code></li>
</ul>

<p>Following diagrams show the different ways of calling <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code>:</p>

<p><img src="/assets/images/notifying.png" alt="Alt text" /></p>

<p>Note the <code class="language-plaintext highlighter-rouge">T1</code> to <code class="language-plaintext highlighter-rouge">T3</code> in the diagram, which indicate the time between the changing of the <em>condition</em> and the calling of the <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code>. As you will see ,what happens inside these time intervals are of crucial importance in deciding the final behavior.</p>

<h2 id="the-matrix">The matrix</h2>

<p>Image that there is one notifying thread and one waiting thread like in our premises. When the notifying thread calls <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code>, what is supposed to happen in our three ways of notifying? Will the waiting thread behaves as expected after the calling of <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code>?</p>

<p>It turns out that these are really complex questions and it needs a matrix to express all the possibilities:</p>

<p><img src="/assets/images/matrix.png" alt="Alt text" /></p>

<p>Just like said earlier, what happens between <em>modifying of the condition</em> and <em>calling of <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code></em> is crucial to understand this table. Let’s explain all the possibilities one by one.</p>

<h3 id="t1-scenario">T1 scenario</h3>

<p>In this scenario, the notifying thread and waiting thread are not influenced by each other through the <code class="language-plaintext highlighter-rouge">mtx</code>(even though the <em>condition</em> itself is atomic, it does not change the result here). When the notifying thread calls <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code>, the waiting thread might be in:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">CP1</code>: This is OK, since the <em>condition</em> has been changed by notifying thread</li>
  <li><code class="language-plaintext highlighter-rouge">CP2</code>: This is OK, same as <code class="language-plaintext highlighter-rouge">CP1</code></li>
  <li><code class="language-plaintext highlighter-rouge">CP3</code>: This need a litte more words. Here means the check of <em>condition</em> fails, which indicate that the waiting thread checked the <em>condition</em> before changing of the <em>condition</em> by the notifying thread. The waiting thread will go to blocking state and miss the notification by the waiting thread. As the promise says that our notifying thread will notify only once, this means that the waiting thread will wait forever!</li>
  <li><code class="language-plaintext highlighter-rouge">CP4</code>, <code class="language-plaintext highlighter-rouge">CPb</code>: Behave as expected, since waiting thread already in blocking state and the notification will work as usual.
    <blockquote>
      <p><code class="language-plaintext highlighter-rouge">CP4</code> and <code class="language-plaintext highlighter-rouge">CPb</code> are completed atomically! They can be seen as same state, I intentionally split into two checkpoints to demonstrate this point.</p>
    </blockquote>
  </li>
  <li><code class="language-plaintext highlighter-rouge">CP5</code>, <code class="language-plaintext highlighter-rouge">CP6</code>: Not possible, since the premise says that there is only one notification call.</li>
  <li><code class="language-plaintext highlighter-rouge">CP7</code>~<code class="language-plaintext highlighter-rouge">CP8</code>: This is OK, the waiting thread check the <em>condition</em> after it has been changed by notifying thread.</li>
</ul>

<h3 id="t2-scenario">T2 scenario</h3>

<p>The notifying thread hold the mutex when changing the <em>condition</em>, which means:</p>

<ul>
  <li>During the time interval <code class="language-plaintext highlighter-rouge">T2</code>, waiting thread must start at <em>unlock</em> state of the <code class="language-plaintext highlighter-rouge">mtx</code>, namely, <code class="language-plaintext highlighter-rouge">CP1</code> and <code class="language-plaintext highlighter-rouge">CP8</code>:</li>
</ul>

<p>Let’s discuss all the checkpoints case by case:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">CP1</code>: This is OK</li>
  <li><code class="language-plaintext highlighter-rouge">CP2</code>: This is OK, waiting thread get the lock after notifying thread releasing the lock</li>
  <li><code class="language-plaintext highlighter-rouge">CP3</code>: This is <strong>NOT</strong> possible! This is the trickist part of all situations. Let me explain it step by step:
    <ul>
      <li>If at the time of the notifying thread calling <code class="language-plaintext highlighter-rouge">notify</code>, the waiting thread is at <code class="language-plaintext highlighter-rouge">CP3</code>, the waiting thread might hold the <code class="language-plaintext highlighter-rouge">mtx</code> <strong>before</strong> or <strong>after</strong> the notifying thread’s holding of the lock when changing the <em>condition</em>
        <ul>
          <li><strong>Before</strong> case: it should have go on to <code class="language-plaintext highlighter-rouge">CP4</code>, so it’s a contradiction</li>
          <li><strong>After</strong> case: it is not possible since the <em>condition</em> had already been changed when waiting thread get the <code class="language-plaintext highlighter-rouge">mtx</code>, it should have go to <code class="language-plaintext highlighter-rouge">CP7</code>, instead of <code class="language-plaintext highlighter-rouge">CP3</code>!!</li>
        </ul>
      </li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">CP4</code>, <code class="language-plaintext highlighter-rouge">CPb</code>: This is OK. This happens if waiting thread check the <em>condition</em> before it being changed. It works as usual, the waiting thread will be woken up.</li>
  <li><code class="language-plaintext highlighter-rouge">CP5</code>, <code class="language-plaintext highlighter-rouge">CP6</code>: Not possible, since the premise says that there is only one notification call.</li>
  <li><code class="language-plaintext highlighter-rouge">CP7</code>~<code class="language-plaintext highlighter-rouge">CP8</code>: This is OK, the waiting thread check the <em>condition</em> after it has been changed by notifying thread.</li>
</ul>

<h3 id="t3-scenario">T3 scenario</h3>

<p>The waiting thread changes the <em>condition</em> and call <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code> while holding the <code class="language-plaintext highlighter-rouge">mtx</code>.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">CP1</code>: This is OK</li>
  <li><code class="language-plaintext highlighter-rouge">CP2</code>: Not possible, since when calling <code class="language-plaintext highlighter-rouge">notify_one/notify_all</code>, notifying thread is holding the <code class="language-plaintext highlighter-rouge">mtx</code></li>
  <li><code class="language-plaintext highlighter-rouge">CP3</code>: Same as <code class="language-plaintext highlighter-rouge">CP2</code></li>
  <li><code class="language-plaintext highlighter-rouge">CP4</code>, <code class="language-plaintext highlighter-rouge">CPb</code>: This is OK. This happens when the waiting thread is firstly get the <code class="language-plaintext highlighter-rouge">mtx</code> and go to blocking state; then the notifying thread get the <code class="language-plaintext highlighter-rouge">mtx</code>. In this situation, the waiting thread will be woken from the kernel, but immedidately be blocked since the notifying thread is holding the lock. Only after the notifying thread release the lock, the waiting thread will be woken again to get the same lock.</li>
  <li><code class="language-plaintext highlighter-rouge">CP5</code>: Not possible, since the premise says that there is only one notification call.</li>
  <li><code class="language-plaintext highlighter-rouge">CP6</code>: Not possible, same reason as <code class="language-plaintext highlighter-rouge">CP5</code>. Even without the same reason for <code class="language-plaintext highlighter-rouge">CP5</code>, <code class="language-plaintext highlighter-rouge">CP6</code> is also impossible, since notifying thread is holding the same lock.</li>
  <li><code class="language-plaintext highlighter-rouge">CP7</code>: Not possible, since notifying thread is holding the same lock</li>
  <li><code class="language-plaintext highlighter-rouge">CP8</code>: Not possible, since the premise says that there is only one notification call. There is no way the waiting thread has already passed the <em>condition</em> by now</li>
</ul>

<h1 id="golden-rules">Golden rules</h1>

<ol>
  <li>Always use <code class="language-plaintext highlighter-rouge">condition_variable</code> with a <em>condition</em>, otherwise there is no way to check whether it’s a spurious wake up or not</li>
  <li>Use the same lock to protect:
    <ol>
      <li>Anything that might change the <em>condition</em>, even when the <em>condition</em> itself is atomic. As the analysis above, the atomic nature of the <em>condition</em> does not change the analysing results.</li>
      <li>Anytime checking the <em>condition</em></li>
      <li>The <em>futex</em>(realized by passing it to <code class="language-plaintext highlighter-rouge">wait(..)</code>)</li>
    </ol>
  </li>
</ol>

<h1 id="real-world-example">Real world example</h1>

<p>Following is an implementation of thread pool, it basically shows every respect of the usage of <code class="language-plaintext highlighter-rouge">condition_variable</code>:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;cstddef&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;condition_variable&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;mutex&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;queue&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;functional&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;thread&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;vector&gt;</span><span class="cp">
</span>
<span class="c1">/// In this example the expression: stop || !tasks.empty() is the 'condition' between notifying</span>
<span class="c1">/// thread and waiting thread. The condition is used to avoid spurious wakeups. Every time the notifying</span>
<span class="c1">/// thread wants to do something that modifies the result of the condition, the mutex must be aquired.</span>
<span class="c1">/// The notify_one/notify_all can be called during the hold of the mutex, or can be called after the</span>
<span class="c1">/// release of the mutex, the difference is that if called during the hold of the lock, the waiting thread</span>
<span class="c1">/// will immediately blocked to wait for the release of the mutex by the calling thread here.</span>

<span class="k">class</span> <span class="nc">ThreadPool</span> <span class="p">{</span>
  <span class="nl">public:</span>
    <span class="n">ThreadPool</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="kt">size_t</span> <span class="n">thread_num</span><span class="p">)</span> <span class="o">:</span> <span class="n">stop</span><span class="p">(</span><span class="nb">false</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">thread_num</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">threads</span><span class="p">.</span><span class="n">emplace_back</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="kr">thread</span><span class="p">(</span><span class="o">&amp;</span><span class="n">ThreadPool</span><span class="o">::</span><span class="n">thread_func</span><span class="p">,</span> <span class="k">this</span><span class="p">));</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="o">~</span><span class="n">ThreadPool</span><span class="p">()</span> <span class="p">{</span>
        <span class="c1">// aquire lock before changing the condition</span>
        <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">unique_lock</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lock</span><span class="p">(</span><span class="n">m</span><span class="p">);</span>
            <span class="n">stop</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
        <span class="p">}</span>

        <span class="n">cv</span><span class="p">.</span><span class="n">notify_all</span><span class="p">();</span>
        <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="o">&amp;</span><span class="kr">thread</span> <span class="o">:</span> <span class="n">threads</span><span class="p">)</span> <span class="p">{</span>
            <span class="kr">thread</span><span class="p">.</span><span class="n">join</span><span class="p">();</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="k">template</span> <span class="o">&lt;</span><span class="k">typename</span> <span class="nc">F</span><span class="p">&gt;</span>
    <span class="kt">void</span> <span class="nf">submit_task</span><span class="p">(</span><span class="n">F</span> <span class="o">&amp;&amp;</span><span class="n">func</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// aquire lock before changing the condition</span>
        <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">unique_lock</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lock</span><span class="p">(</span><span class="n">m</span><span class="p">);</span>
            <span class="n">tasks</span><span class="p">.</span><span class="n">emplace</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">forward</span><span class="o">&lt;</span><span class="n">F</span><span class="o">&gt;</span><span class="p">(</span><span class="n">func</span><span class="p">));</span>
        <span class="p">}</span>
        <span class="c1">// call notify_all after release the lock; this will prevent the waiting thread from immediate block</span>
        <span class="c1">// after wake up</span>
        <span class="n">cv</span><span class="p">.</span><span class="n">notify_one</span><span class="p">();</span>
    <span class="p">}</span>

  <span class="k">private</span><span class="o">:</span>
    <span class="kt">void</span> <span class="nf">thread_func</span><span class="p">()</span> <span class="p">{</span>
        <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">function</span><span class="o">&lt;</span><span class="kt">void</span><span class="p">()</span><span class="o">&gt;</span> <span class="n">task</span><span class="p">;</span>
            <span class="p">{</span>
                <span class="n">std</span><span class="o">::</span><span class="n">unique_lock</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lock</span><span class="p">(</span><span class="n">m</span><span class="p">);</span>
                <span class="c1">// wait for the condition to be true; condition is required. If no condtion, waiting thread</span>
                <span class="c1">// can not decide it's spurious wake-up or not</span>
                <span class="n">cv</span><span class="p">.</span><span class="n">wait</span><span class="p">(</span><span class="n">lock</span><span class="p">,</span> <span class="p">[</span><span class="k">this</span><span class="p">]()</span> <span class="p">{</span> <span class="k">return</span> <span class="n">stop</span> <span class="o">||</span> <span class="o">!</span><span class="n">tasks</span><span class="p">.</span><span class="n">empty</span><span class="p">();</span> <span class="p">});</span>
                <span class="k">if</span> <span class="p">(</span><span class="n">stop</span> <span class="o">&amp;&amp;</span> <span class="n">tasks</span><span class="p">.</span><span class="n">empty</span><span class="p">())</span> <span class="p">{</span>
                    <span class="k">return</span><span class="p">;</span>
                <span class="p">}</span>
                <span class="c1">// protected under the mutex, since these steps will change the condition</span>
                <span class="n">task</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">tasks</span><span class="p">.</span><span class="n">front</span><span class="p">());</span>
                <span class="n">tasks</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
            <span class="p">}</span>
            <span class="c1">// do not influence the condition, do it without holding the mutext</span>
            <span class="n">task</span><span class="p">();</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="n">std</span><span class="o">::</span><span class="n">queue</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">function</span><span class="o">&lt;</span><span class="kt">void</span><span class="p">()</span><span class="o">&gt;&gt;</span> <span class="n">tasks</span><span class="p">;</span>
    <span class="c1">// This mutex is used for three purpose and these three purposes must be protected under this same mutex:</span>
    <span class="c1">// 1. Protect the tasks queue</span>
    <span class="c1">// 2. Protect the stop condition</span>
    <span class="c1">// 3. Protect the condition variable(which is a futex)</span>
    <span class="c1">// 1 and 2 both influence the result of the condition; 3 is required by the futex implementation in syscall</span>
    <span class="n">std</span><span class="o">::</span><span class="n">mutex</span> <span class="n">m</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">condition_variable</span> <span class="n">cv</span><span class="p">;</span>
    <span class="kt">bool</span> <span class="n">stop</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kr">thread</span><span class="o">&gt;</span> <span class="n">threads</span><span class="p">;</span>
<span class="p">};</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// used to sync std::cout</span>
    <span class="n">std</span><span class="o">::</span><span class="n">mutex</span> <span class="n">m</span><span class="p">;</span>
    <span class="n">ThreadPool</span> <span class="n">pool</span><span class="p">(</span><span class="mi">4</span><span class="p">);</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="mi">8</span><span class="p">;</span> <span class="n">i</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">pool</span><span class="p">.</span><span class="n">submit_task</span><span class="p">([</span><span class="n">i</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">m</span><span class="p">]()</span> <span class="p">{</span>
            <span class="n">std</span><span class="o">::</span><span class="n">lock_guard</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lock</span><span class="p">{</span><span class="n">m</span><span class="p">};</span>
            <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Task "</span> <span class="o">&lt;&lt;</span> <span class="n">i</span> <span class="o">&lt;&lt;</span> <span class="s">" is running</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
        <span class="p">});</span>
    <span class="p">}</span>
    <span class="n">std</span><span class="o">::</span><span class="n">this_thread</span><span class="o">::</span><span class="n">sleep_for</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">chrono</span><span class="o">::</span><span class="n">seconds</span><span class="p">(</span><span class="mi">5</span><span class="p">));</span>

    <span class="n">pool</span><span class="p">.</span><span class="n">submit_task</span><span class="p">([</span><span class="o">&amp;</span><span class="n">m</span><span class="p">]()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">lock_guard</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">mutex</span><span class="o">&gt;</span> <span class="n">lock</span><span class="p">{</span><span class="n">m</span><span class="p">};</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"This is the last task"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="p">});</span>

    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>

</code></pre></div></div>]]></content><author><name></name></author><category term="C++" /><summary type="html"><![CDATA[This post tries to enumerate the pitfalls that might happen in the use of condition_variable. First I will explain scenarios in the use of condition_variable; then give a summary about tips of using it; At the end of the post, a live example is given to demonstrate what have been talked about in this post. Hope this will help someone to spent one less miniute debugging condition_variable.]]></summary></entry><entry><title type="html">smartPtr构造&amp;amp;析构行为</title><link href="http://localhost:4000/2024/04/21/smartPtr-%E6%9E%84%E9%80%A0-%E6%9E%90%E6%9E%84%E8%A1%8C%E4%B8%BA.html" rel="alternate" type="text/html" title="smartPtr构造&amp;amp;析构行为" /><published>2024-04-21T12:22:46+08:00</published><updated>2024-04-21T12:22:46+08:00</updated><id>http://localhost:4000/2024/04/21/smartPtr-%E6%9E%84%E9%80%A0-%E6%9E%90%E6%9E%84%E8%A1%8C%E4%B8%BA</id><content type="html" xml:base="http://localhost:4000/2024/04/21/smartPtr-%E6%9E%84%E9%80%A0-%E6%9E%90%E6%9E%84%E8%A1%8C%E4%B8%BA.html"><![CDATA[<p>在<a href="https://shan-weiqiang.github.io/2024/04/20/smartPtr-%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B.html">smartPtr内存模型</a>文章中，介绍了<code class="language-plaintext highlighter-rouge">std::shared_ptr</code>和<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>的内存模型区别，这篇文章介绍它们的构造和析构的行为区别。</p>

<blockquote>
  <p>注意：本文只讨论使用默认删除器的情况，如果使用自定义删除器，则跟删除器的实现有关系，本文讨论的前提和结论可能都不再成立！</p>
</blockquote>

<ul id="markdown-toc">
  <li><a href="#参考文章" id="markdown-toc-参考文章">参考文章</a></li>
  <li><a href="#模板参数类型的完整性" id="markdown-toc-模板参数类型的完整性">模板参数类型的完整性</a></li>
  <li><a href="#析构行为" id="markdown-toc-析构行为">析构行为</a></li>
</ul>

<h1 id="参考文章">参考文章</h1>

<ol>
  <li><a href="https://en.cppreference.com/w/cpp/memory/unique_ptr">https://en.cppreference.com/w/cpp/memory/unique_ptr</a></li>
</ol>

<blockquote>
  <p>std::unique_ptr may be constructed for an incomplete type T, such as to facilitate the use as a handle in the pImpl idiom. If the default deleter is used, T must be complete at the point in code where the deleter is invoked, which happens in the destructor, move assignment operator, and reset member function of std::unique_ptr. (Conversely, std::shared_ptr can’t be constructed from a raw pointer to incomplete type, but can be destroyed where T is incomplete).</p>

  <p>If T is a derived class of some base B, then std::unique_ptr<T> is implicitly convertible to std::unique_ptr<b>. The default deleter of the resulting std::unique_ptr<b> will use operator delete for B, leading to undefined behavior unless the destructor of B is virtual. Note that std::shared_ptr behaves differently: std::shared_ptr<b> will use the operator delete for the type T and the owned object will be deleted correctly even if the destructor of B is not virtual.</b></b></b></T></p>
</blockquote>

<h1 id="模板参数类型的完整性">模板参数类型的完整性</h1>

<ul>
  <li><code class="language-plaintext highlighter-rouge">std::shared_ptr</code>构造时要求模板参数类型是complete type，析构时没有此要求
    <ul>
      <li>因为在<code class="language-plaintext highlighter-rouge">std::shared_ptr</code>构造的过程中需要创建Control Block，而在Control Block中需要存储被管理类型的析构函数</li>
      <li>而在析构时，无论当前的<code class="language-plaintext highlighter-rouge">shared_ptr</code>存储的类型是否complete，它指向的Control Block肯定是有完整的deleter的，因为这是在<code class="language-plaintext highlighter-rouge">shared_ptr</code>构造时就保证的</li>
    </ul>
  </li>
  <li><code class="language-plaintext highlighter-rouge">std::unique_ptr</code>构造时对模板参数类型没有要求，析构时要求必须是complete type
    <ul>
      <li><code class="language-plaintext highlighter-rouge">unique_ptr</code>没有Control Block，无需在构造时存储管理类型的deleter</li>
      <li>而在析构时，<code class="language-plaintext highlighter-rouge">unique_ptr</code>需要根据当前类型来调用管理类型的析构函数来释放资源</li>
    </ul>
  </li>
</ul>

<p>下面这个例子从侧面证明这一点：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="k">class</span> <span class="nc">Incomplete</span><span class="p">;</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">Incomplete</span> <span class="o">*</span><span class="n">raw</span><span class="p">;</span>
    <span class="cm">/**
    In file included from /usr/include/c++/11/bits/shared_ptr.h:53,
                     from /usr/include/c++/11/memory:77,
                     from incomplete_type.cpp:1:
    /usr/include/c++/11/bits/shared_ptr_base.h: In instantiation of ‘std::__shared_ptr&lt;_Tp, _Lp&gt;::__shared_ptr(_Yp*) [with _Yp = Incomplete; &lt;template-parameter-2-2&gt; = void; _Tp = Incomplete; __gnu_cxx::_Lock_policy _Lp = __gnu_cxx::_S_atomic]’:
    /usr/include/c++/11/bits/shared_ptr.h:160:46:   required from ‘std::shared_ptr&lt;_Tp&gt;::shared_ptr(_Yp*) [with _Yp = Incomplete; &lt;template-parameter-2-2&gt; = void; _Tp = Incomplete]’
    incomplete_type.cpp:8:46:   required from here
    /usr/include/c++/11/bits/shared_ptr_base.h:1102:26: error: invalid application of ‘sizeof’ to incomplete type ‘Incomplete’
     1102 |           static_assert( sizeof(_Yp) &gt; 0, "incomplete type" );
          |                          ^~~~~~~~~~~
    In file included from /usr/include/c++/11/memory:76,
                     from incomplete_type.cpp:1:
    /usr/include/c++/11/bits/unique_ptr.h: In instantiation of ‘void std::default_delete&lt;_Tp&gt;::operator()(_Tp*) const [with _Tp = Incomplete]’:
    /usr/include/c++/11/bits/unique_ptr.h:361:17:   required from ‘std::unique_ptr&lt;_Tp, _Dp&gt;::~unique_ptr() [with _Tp = Incomplete; _Dp = std::default_delete&lt;Incomplete&gt;]’
    incomplete_type.cpp:9:47:   required from here
    /usr/include/c++/11/bits/unique_ptr.h:83:23: error: invalid application of ‘sizeof’ to incomplete type ‘Incomplete’
       83 |         static_assert(sizeof(_Tp)&gt;0,
          |                       ^~~~~~~~~~~
    */</span>

    <span class="cm">/**
     * The following code will not compile because the type Incomplete is incomplete.
     * But std::shared_ptr and std::unique_ptr fail for different reasons.
     * As for std::shared_ptr, it requires the complete type of the template argument in constructor.
     *
     * As for std::unique_ptr, it requires the complete type of the template argument in destructor.
     */</span>
    <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">Incomplete</span><span class="o">&gt;</span> <span class="n">share_ptr</span><span class="p">(</span><span class="n">raw</span><span class="p">);</span>
    <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Incomplete</span><span class="o">&gt;</span> <span class="n">unique_ptr</span><span class="p">(</span><span class="n">raw</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>在<a href="https://shan-weiqiang.github.io/2023/12/24/pimpl%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8Funique_ptr-incomplete-type-%E9%97%AE%E9%A2%98.html">pimpl设计模式unique_ptr incomplete type问题</a>，对<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>的<code class="language-plaintext highlighter-rouge">incomplete type</code>问题做了全面的分析。</p>

<h1 id="析构行为">析构行为</h1>

<ul>
  <li><code class="language-plaintext highlighter-rouge">shared_ptr</code>无论如何都会正确的调用其管理对象的析构函数</li>
  <li><code class="language-plaintext highlighter-rouge">unique_ptr</code>不保证能正确的析构
    <ul>
      <li>只有在被管理对象有继承关系，且析构是<code class="language-plaintext highlighter-rouge">virtual</code>时才能保证</li>
    </ul>
  </li>
</ul>

<p>如下的例子说明这一点：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="k">struct</span> <span class="nc">A</span> <span class="p">{</span>

    <span class="o">~</span><span class="n">A</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"A destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="k">struct</span> <span class="nc">B</span> <span class="o">:</span> <span class="k">public</span> <span class="n">A</span> <span class="p">{</span>

    <span class="o">~</span><span class="n">B</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"B destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="k">struct</span> <span class="nc">C</span> <span class="p">{</span>

    <span class="k">virtual</span> <span class="o">~</span><span class="n">C</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"C destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="k">struct</span> <span class="nc">D</span> <span class="o">:</span> <span class="k">public</span> <span class="n">C</span> <span class="p">{</span>

    <span class="o">~</span><span class="n">D</span><span class="p">()</span> <span class="k">override</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"D destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"When desctructor is non-virtual, std::shared_ptr will call correct destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
        <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span> <span class="n">a</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">B</span><span class="o">&gt;</span><span class="p">();</span>
    <span class="p">}</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"----"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"When destructor is virtual, std::shared_ptr will call correct destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
        <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">C</span><span class="o">&gt;</span> <span class="n">b</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">D</span><span class="o">&gt;</span><span class="p">();</span>
    <span class="p">}</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"----"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"When destructor is non-virtual, std::unique_ptr will NOT call correct destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
        <span class="c1">/// This is undefined behavior, since B is destructed by A's destructor</span>
        <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span> <span class="n">c</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_unique</span><span class="o">&lt;</span><span class="n">B</span><span class="o">&gt;</span><span class="p">();</span>
    <span class="p">}</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"----"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"When destructor is virtual, std::unique_ptr will call correct destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
        <span class="c1">/// Since destructor is virtual, D's destructor will be called</span>
        <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">C</span><span class="o">&gt;</span> <span class="n">d</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_unique</span><span class="o">&lt;</span><span class="n">D</span><span class="o">&gt;</span><span class="p">();</span>
    <span class="p">}</span>
<span class="p">}</span>

</code></pre></div></div>]]></content><author><name></name></author><category term="C++" /><summary type="html"><![CDATA[在smartPtr内存模型文章中，介绍了std::shared_ptr和std::unique_ptr的内存模型区别，这篇文章介绍它们的构造和析构的行为区别。]]></summary></entry><entry><title type="html">smartPtr内存模型</title><link href="http://localhost:4000/2024/04/20/smartPtr-%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B.html" rel="alternate" type="text/html" title="smartPtr内存模型" /><published>2024-04-20T12:22:46+08:00</published><updated>2024-04-20T12:22:46+08:00</updated><id>http://localhost:4000/2024/04/20/smartPtr-%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B</id><content type="html" xml:base="http://localhost:4000/2024/04/20/smartPtr-%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B.html"><![CDATA[<p><code class="language-plaintext highlighter-rouge">shared_ptr</code>的删除器默认为<code class="language-plaintext highlighter-rouge">delete</code>操作符，但是由于<code class="language-plaintext highlighter-rouge">shared_ptr</code>指向的对象可能与控制块指向的对象不一致，所以删除器删除的对象可能与预期的不一致。理解智能指针的内存模型后，对理解这些行为很有帮助。</p>

<ul id="markdown-toc">
  <li><a href="#参考文章" id="markdown-toc-参考文章">参考文章</a></li>
  <li><a href="#shared指针行为" id="markdown-toc-shared指针行为">shared指针行为</a>    <ul>
      <li><a href="#shared_ptr内存布局" id="markdown-toc-shared_ptr内存布局">shared_ptr内存布局</a></li>
      <li><a href="#指针位置相同类型不同" id="markdown-toc-指针位置相同类型不同">指针位置相同，类型不同</a></li>
      <li><a href="#aliasing-constructor" id="markdown-toc-aliasing-constructor">aliasing constructor</a></li>
      <li><a href="#get的返回值类型" id="markdown-toc-get的返回值类型"><code class="language-plaintext highlighter-rouge">get()</code>的返回值类型</a></li>
      <li><a href="#cast操作" id="markdown-toc-cast操作">Cast操作</a></li>
    </ul>
  </li>
  <li><a href="#unique指针行为" id="markdown-toc-unique指针行为">unique指针行为</a>    <ul>
      <li><a href="#unique_ptr内存布局" id="markdown-toc-unique_ptr内存布局">unique_ptr内存布局</a></li>
    </ul>
  </li>
</ul>

<h1 id="参考文章">参考文章</h1>

<p>本文参考了如下文章：</p>

<ol>
  <li><a href="https://en.cppreference.com/w/cpp/memory/shared_ptr/shared_ptr">https://en.cppreference.com/w/cpp/memory/shared_ptr/shared_ptr)</a></li>
  <li><a href="https://www.nextptr.com/tutorial/ta1358374985/shared_ptr-basics-and-internals-with-examples">https://www.nextptr.com/tutorial/ta1358374985/shared_ptr-basics-and-internals-with-examples</a></li>
  <li><a href="https://www.nextptr.com/question/qa1355324734/aliasing-constructed-shared_ptr-as-key-of-map-or-set">https://www.nextptr.com/question/qa1355324734/aliasing-constructed-shared_ptr-as-key-of-map-or-set</a></li>
  <li><a href="https://en.cppreference.com/w/cpp/memory/shared_ptr/pointer_cast">https://en.cppreference.com/w/cpp/memory/shared_ptr/pointer_cast</a></li>
</ol>

<h1 id="shared指针行为">shared指针行为</h1>

<h2 id="shared_ptr内存布局">shared_ptr内存布局</h2>

<p>如下图是<code class="language-plaintext highlighter-rouge">std::shared_ptr</code>指针的内存layout：</p>

<p><img src="/assets/images/control.png" alt="Alt text" /></p>

<p><code class="language-plaintext highlighter-rouge">shared_ptr</code>的删除器是通过构造函数的方式传入的，不是<code class="language-plaintext highlighter-rouge">shared_ptr</code>智能指针类型的一部分。所以不同删除器的管理相同类型的智能指针可以放在同一个容器中(因为管理对象有相同的类型)。如果不提供删除器，则使用默认的<code class="language-plaintext highlighter-rouge">delete</code>操作符删除对象。</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// file: /usr/include/c++/11/bits/shared_ptr_base.h</span>
  <span class="c1">// Counted ptr with no deleter or allocator support</span>
  <span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="nc">_Ptr</span><span class="p">,</span> <span class="n">_Lock_policy</span> <span class="n">_Lp</span><span class="p">&gt;</span>
    <span class="k">class</span> <span class="nc">_Sp_counted_ptr</span> <span class="k">final</span> <span class="o">:</span> <span class="k">public</span> <span class="n">_Sp_counted_base</span><span class="o">&lt;</span><span class="n">_Lp</span><span class="o">&gt;</span>
    <span class="p">{</span>
<span class="p">...</span>
      <span class="k">virtual</span> <span class="kt">void</span>
      <span class="n">_M_dispose</span><span class="p">()</span> <span class="k">noexcept</span>
      <span class="p">{</span> <span class="k">delete</span> <span class="n">_M_ptr</span><span class="p">;</span> <span class="p">}</span> <span class="c1">// 默认直接调用delete</span>
<span class="p">...</span>
      <span class="k">virtual</span> <span class="kt">void</span><span class="o">*</span>
      <span class="n">_M_get_deleter</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">type_info</span><span class="o">&amp;</span><span class="p">)</span> <span class="k">noexcept</span>
      <span class="p">{</span> <span class="k">return</span> <span class="nb">nullptr</span><span class="p">;</span> <span class="p">}</span> <span class="c1">// deleter 返回空指针</span>
<span class="p">...</span>
    <span class="k">private</span><span class="o">:</span>
      <span class="n">_Ptr</span>             <span class="n">_M_ptr</span><span class="p">;</span>
    <span class="p">};</span>

    <span class="p">...</span>

  <span class="c1">// Support for custom deleter and/or allocator</span>
  <span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="nc">_Ptr</span><span class="p">,</span> <span class="k">typename</span> <span class="nc">_Deleter</span><span class="p">,</span> <span class="k">typename</span> <span class="nc">_Alloc</span><span class="p">,</span> <span class="n">_Lock_policy</span> <span class="n">_Lp</span><span class="p">&gt;</span>
    <span class="k">class</span> <span class="nc">_Sp_counted_deleter</span> <span class="k">final</span> <span class="o">:</span> <span class="k">public</span> <span class="n">_Sp_counted_base</span><span class="o">&lt;</span><span class="n">_Lp</span><span class="o">&gt;</span>
    <span class="p">{</span>
      <span class="k">class</span> <span class="nc">_Impl</span> <span class="o">:</span> <span class="n">_Sp_ebo_helper</span><span class="o">&lt;</span><span class="mi">0</span><span class="p">,</span> <span class="n">_Deleter</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">_Sp_ebo_helper</span><span class="o">&lt;</span><span class="mi">1</span><span class="p">,</span> <span class="n">_Alloc</span><span class="o">&gt;</span>
      <span class="p">{</span>
<span class="p">...</span>
	<span class="n">_Deleter</span><span class="o">&amp;</span> <span class="n">_M_del</span><span class="p">()</span> <span class="k">noexcept</span> <span class="p">{</span> <span class="k">return</span> <span class="n">_Del_base</span><span class="o">::</span><span class="n">_S_get</span><span class="p">(</span><span class="o">*</span><span class="k">this</span><span class="p">);</span> <span class="p">}</span>
	<span class="n">_Alloc</span><span class="o">&amp;</span> <span class="n">_M_alloc</span><span class="p">()</span> <span class="k">noexcept</span> <span class="p">{</span> <span class="k">return</span> <span class="n">_Alloc_base</span><span class="o">::</span><span class="n">_S_get</span><span class="p">(</span><span class="o">*</span><span class="k">this</span><span class="p">);</span> <span class="p">}</span>
	<span class="n">_Ptr</span> <span class="n">_M_ptr</span><span class="p">;</span>
      <span class="p">};</span>
<span class="p">...</span>
      <span class="k">virtual</span> <span class="kt">void</span>
      <span class="nf">_M_dispose</span><span class="p">()</span> <span class="k">noexcept</span>
      <span class="p">{</span> <span class="n">_M_impl</span><span class="p">.</span><span class="n">_M_del</span><span class="p">()(</span><span class="n">_M_impl</span><span class="p">.</span><span class="n">_M_ptr</span><span class="p">);</span> <span class="p">}</span> <span class="c1">// 先获取deleter，然后将指针传入</span>
<span class="p">...</span>
    <span class="k">private</span><span class="o">:</span>
      <span class="n">_Impl</span> <span class="n">_M_impl</span><span class="p">;</span>
    <span class="p">};</span>
</code></pre></div></div>

<p>下面这个例子说明了<code class="language-plaintext highlighter-rouge">deleter</code>的行为：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;vector&gt;</span><span class="cp">
</span>
<span class="k">struct</span> <span class="nc">SampleType</span> <span class="p">{</span>
    <span class="n">SampleType</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Constructor called</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="o">~</span><span class="n">SampleType</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Destructor called</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">SampleType</span><span class="o">&gt;</span> <span class="n">ptr</span><span class="p">(</span><span class="k">new</span> <span class="n">SampleType</span><span class="p">(),</span> <span class="p">[](</span><span class="n">SampleType</span> <span class="o">*</span><span class="n">p</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Custom deleter called</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
        <span class="k">delete</span> <span class="n">p</span><span class="p">;</span>
    <span class="p">});</span>
    <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">SampleType</span><span class="o">&gt;</span> <span class="n">normal_ptr</span><span class="p">(</span><span class="k">new</span> <span class="nf">SampleType</span><span class="p">());</span>

    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">SampleType</span><span class="o">&gt;&gt;</span> <span class="n">vec</span><span class="p">;</span>
    <span class="n">vec</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">ptr</span><span class="p">);</span>
    <span class="n">vec</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">normal_ptr</span><span class="p">);</span>

    <span class="c1">// ptr and normal_ptr are of the same type</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"total:"</span> <span class="o">&lt;&lt;</span> <span class="n">vec</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<p>输出：</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Constructor called
Constructor called
total:2
Destructor called
Custom deleter cal
</code></pre></div></div>

<blockquote>
  <p>注意：<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>的<code class="language-plaintext highlighter-rouge">deleter</code>是模板参数，是其类型的一部分</p>
</blockquote>

<h2 id="指针位置相同类型不同">指针位置相同，类型不同</h2>

<p>这种情况对应的是图中的<code class="language-plaintext highlighter-rouge">shared_ptr&lt;B&gt; three</code>，下面是一个例子说明其行为：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
</span>
<span class="k">struct</span> <span class="nc">BaseType</span> <span class="p">{</span>
    <span class="n">BaseType</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"BaseType constructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">virtual</span> <span class="o">~</span><span class="n">BaseType</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"BaseType destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="k">struct</span> <span class="nc">DerivedType</span> <span class="o">:</span> <span class="k">public</span> <span class="n">BaseType</span> <span class="p">{</span>
    <span class="n">DerivedType</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"DerivedType constructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="o">~</span><span class="n">DerivedType</span><span class="p">()</span> <span class="k">override</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"DerivedType destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="c1">// 尽管ptr是std::shared_ptr&lt;BaseType&gt;类型，但是因为control block指向的是DerivedType对象，所以DerivedType的析构函数会被调用</span>
    <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">BaseType</span><span class="o">&gt;</span> <span class="n">ptr</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">DerivedType</span><span class="o">&gt;</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>
<p>输出：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>BaseType constructor
DerivedType constructor
DerivedType destructor
BaseType destructor
</code></pre></div></div>

<h2 id="aliasing-constructor">aliasing constructor</h2>

<p>对应图中<code class="language-plaintext highlighter-rouge">shared_ptr&lt;C&gt; four</code>的情况，这种情况下其指向的对象不会在智能指针析构时释放资源。</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="k">struct</span> <span class="nc">A</span> <span class="p">{</span>
    <span class="n">A</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"A constructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="o">~</span><span class="n">A</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"A destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="k">struct</span> <span class="nc">B</span> <span class="p">{</span>
    <span class="n">B</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"B constructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="o">~</span><span class="n">B</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"B destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="k">struct</span> <span class="nc">C</span> <span class="p">{</span>
    <span class="n">C</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"C constructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="o">~</span><span class="n">C</span><span class="p">()</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"C destructor</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="n">A</span> <span class="n">a</span><span class="p">;</span>
    <span class="n">B</span> <span class="n">b</span><span class="p">;</span>
<span class="p">};</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span> <span class="n">a_ptr</span><span class="p">;</span>
    <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">C</span><span class="o">&gt;</span> <span class="n">ptr</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_shared</span><span class="o">&lt;</span><span class="n">C</span><span class="o">&gt;</span><span class="p">();</span>
        <span class="c1">// a_ptr shared ref count with ptr, but point to ptr.a</span>
        <span class="n">a_ptr</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">A</span><span class="o">&gt;</span><span class="p">{</span><span class="n">ptr</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">ptr</span><span class="o">-&gt;</span><span class="n">a</span><span class="p">};</span>
    <span class="p">}</span>
    <span class="c1">// ptr is out of scope, but object it points to is not destructed yet,since a_ptr still holds a reference to ptr.a</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"C's construct should not be destructed yet</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
<span class="p">}</span>

</code></pre></div></div>

<p>输出：</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A constructor
B constructor
C constructor
C's construct should not be destructed yet
C destructor
B destructor
A destructor
</code></pre></div></div>

<h2 id="get的返回值类型"><code class="language-plaintext highlighter-rouge">get()</code>的返回值类型</h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">shared_ptr&lt;A&gt;</code>的<code class="language-plaintext highlighter-rouge">get()</code>返回值类型是<code class="language-plaintext highlighter-rouge">A</code>的指针类型，不一定是Control Block指向的地址</li>
  <li>智能指针的共享所有权与其存储的具体的指向的类型与地址，是独立的，虽然大部分情况下它们是一样的</li>
</ul>

<h2 id="cast操作">Cast操作</h2>

<p>有了上面的理解基础，即智能指针的共享对象和其存储的具体对象类型和地址是独立的，就很自然的会碰到如下情况：</p>

<ul>
  <li>想共享Control Block，但是希望存储的指针被当做另外一个类型</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">std::static_pointer_cast，std::dynamic_pointer_cast，std::const_pointer_cast，std::reinterpret_pointer_cast</code> 用户实现以上操作，其各自的用法与对应的<code class="language-plaintext highlighter-rouge">std::static_cast，std::dynamic_cast，std::const_cast，std::reinterpret_cast</code> 相同，只不过这里是对<code class="language-plaintext highlighter-rouge">shared_ptr</code>类型<code class="language-plaintext highlighter-rouge">get()</code>返回的类型进行操作。</p>

<p><a href="https://en.cppreference.com/w/cpp/memory/shared_ptr/pointer_cast">https://en.cppreference.com/w/cpp/memory/shared_ptr/pointer_cast</a></p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// static_pointer_cast</span>
<span class="k">template</span><span class="o">&lt;</span><span class="k">class</span> <span class="nc">T</span><span class="p">,</span> <span class="k">class</span> <span class="nc">U</span><span class="p">&gt;</span>
<span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="n">static_pointer_cast</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">U</span><span class="o">&gt;&amp;</span> <span class="n">r</span><span class="p">)</span> <span class="k">noexcept</span>
<span class="p">{</span>
    <span class="k">auto</span> <span class="n">p</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">element_type</span><span class="o">*&gt;</span><span class="p">(</span><span class="n">r</span><span class="p">.</span><span class="n">get</span><span class="p">());</span>
    <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span><span class="p">{</span><span class="n">r</span><span class="p">,</span> <span class="n">p</span><span class="p">};</span>
<span class="p">}</span>
<span class="c1">// dynamic_pointer_cast</span>
<span class="k">template</span><span class="o">&lt;</span><span class="k">class</span> <span class="nc">T</span><span class="p">,</span> <span class="k">class</span> <span class="nc">U</span><span class="p">&gt;</span>
<span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="n">dynamic_pointer_cast</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">U</span><span class="o">&gt;&amp;</span> <span class="n">r</span><span class="p">)</span> <span class="k">noexcept</span>
<span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="k">auto</span> <span class="n">p</span> <span class="o">=</span> <span class="k">dynamic_cast</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">element_type</span><span class="o">*&gt;</span><span class="p">(</span><span class="n">r</span><span class="p">.</span><span class="n">get</span><span class="p">()))</span>
        <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span><span class="p">{</span><span class="n">r</span><span class="p">,</span> <span class="n">p</span><span class="p">};</span>
    <span class="k">else</span>
        <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span><span class="p">{};</span>
<span class="p">}</span>
<span class="c1">// const_pointer_cast</span>
<span class="k">template</span><span class="o">&lt;</span><span class="k">class</span> <span class="nc">T</span><span class="p">,</span> <span class="k">class</span> <span class="nc">U</span><span class="p">&gt;</span>
<span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="n">const_pointer_cast</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">U</span><span class="o">&gt;&amp;</span> <span class="n">r</span><span class="p">)</span> <span class="k">noexcept</span>
<span class="p">{</span>
    <span class="k">auto</span> <span class="n">p</span> <span class="o">=</span> <span class="k">const_cast</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">element_type</span><span class="o">*&gt;</span><span class="p">(</span><span class="n">r</span><span class="p">.</span><span class="n">get</span><span class="p">());</span>
    <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span><span class="p">{</span><span class="n">r</span><span class="p">,</span> <span class="n">p</span><span class="p">};</span>
<span class="p">}</span>
<span class="c1">// reinterpret_pointer_cast</span>
<span class="k">template</span><span class="o">&lt;</span><span class="k">class</span> <span class="nc">T</span><span class="p">,</span> <span class="k">class</span> <span class="nc">U</span><span class="p">&gt;</span>
<span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="n">reinterpret_pointer_cast</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">U</span><span class="o">&gt;&amp;</span> <span class="n">r</span><span class="p">)</span> <span class="k">noexcept</span>
<span class="p">{</span>
    <span class="k">auto</span> <span class="n">p</span> <span class="o">=</span> <span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;::</span><span class="n">element_type</span><span class="o">*&gt;</span><span class="p">(</span><span class="n">r</span><span class="p">.</span><span class="n">get</span><span class="p">());</span>
    <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">shared_ptr</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span><span class="p">{</span><span class="n">r</span><span class="p">,</span> <span class="n">p</span><span class="p">};</span>
<span class="p">}</span>

</code></pre></div></div>

<h1 id="unique指针行为">unique指针行为</h1>

<h2 id="unique_ptr内存布局">unique_ptr内存布局</h2>

<p><img src="/assets/images/memory.png" alt="Alt text" /></p>

<p><code class="language-plaintext highlighter-rouge">std::unique_ptr</code>指针的删除器是其类型的一部分，这与<code class="language-plaintext highlighter-rouge">std::shared_ptr</code>是有区别的，并且其内存布局非常简单，没有共享控制块的原子操作。由于删除器是其类型的一部分，且内存模型不同，它和<code class="language-plaintext highlighter-rouge">std::shared_ptr</code>有很多区别，将在<a href="https://shan-weiqiang.github.io/2024/04/21/smartPtr-%E6%9E%84%E9%80%A0-%E6%9E%90%E6%9E%84%E8%A1%8C%E4%B8%BA.html">smartPtr构造&amp;析构行为</a>详细分析。</p>]]></content><author><name></name></author><category term="C++" /><summary type="html"><![CDATA[shared_ptr的删除器默认为delete操作符，但是由于shared_ptr指向的对象可能与控制块指向的对象不一致，所以删除器删除的对象可能与预期的不一致。理解智能指针的内存模型后，对理解这些行为很有帮助。]]></summary></entry><entry><title type="html">SOME/IP 解读</title><link href="http://localhost:4000/2024/04/19/SOME-IP-%E8%A7%A3%E8%AF%BB.html" rel="alternate" type="text/html" title="SOME/IP 解读" /><published>2024-04-19T19:22:46+08:00</published><updated>2024-04-19T19:22:46+08:00</updated><id>http://localhost:4000/2024/04/19/SOME-IP-%E8%A7%A3%E8%AF%BB</id><content type="html" xml:base="http://localhost:4000/2024/04/19/SOME-IP-%E8%A7%A3%E8%AF%BB.html"><![CDATA[<p><a href="https://www.autosar.org/fileadmin/standards/R22-11/FO/AUTOSAR_PRS_SOMEIPProtocol.pdf">SOME/IP</a> 协议是AUTOSAR AP和CP标准中包含的面向服务的通信协议。在AP上，SOME/IP 基于网络传输层的TCP和UDP协议，属于OSI模型的应用层协议。本文不是原文的翻译，而是按照章节对重要的，且难以理解的部分进行解释，希望能帮助到有需要的读者。</p>

<blockquote>
  <p>SOME/IP在AUTOSAR中是通信的基础，在Manifest模型中，非常多的模型定义都跟SOME/IP相关；理解SOME/IP对理解AUTOSAR模型系统很重要</p>

</blockquote>

<ul id="markdown-toc">
  <li><a href="#4-protocol-specification" id="markdown-toc-4-protocol-specification">4 Protocol Specification</a>    <ul>
      <li><a href="#41-specification-of-someip-message-format-serialization" id="markdown-toc-41-specification-of-someip-message-format-serialization">4.1 Specification of SOME/IP Message Format (Serialization)</a>        <ul>
          <li><a href="#412-header" id="markdown-toc-412-header">4.1.2 Header</a>            <ul>
              <li><a href="#4121-message-id-32-bit" id="markdown-toc-4121-message-id-32-bit">4.1.2.1 Message ID [32 Bit]</a></li>
              <li><a href="#4124-request-id-32-bit" id="markdown-toc-4124-request-id-32-bit">4.1.2.4 Request ID [32 Bit]</a></li>
              <li><a href="#4127-message-type-8-bit" id="markdown-toc-4127-message-type-8-bit">4.1.2.7 Message Type [8 Bit]</a></li>
            </ul>
          </li>
          <li><a href="#414-serialization-of-data-structures" id="markdown-toc-414-serialization-of-data-structures">4.1.4 Serialization of Data Structures</a>            <ul>
              <li><a href="#4143-structured-datatypes-and-arguments-with-identifier-and-optional-members-tlv" id="markdown-toc-4143-structured-datatypes-and-arguments-with-identifier-and-optional-members-tlv">4.1.4.3 Structured Datatypes and Arguments with Identifier and optional members (’TLV’)</a></li>
            </ul>
          </li>
        </ul>
      </li>
      <li><a href="#42-specification-of-someip-protocol" id="markdown-toc-42-specification-of-someip-protocol">4.2 Specification of SOME/IP Protocol</a>        <ul>
          <li><a href="#421-transport-protocol-bindings" id="markdown-toc-421-transport-protocol-bindings">4.2.1 Transport Protocol Bindings</a>            <ul>
              <li><a href="#4211-udp-binding" id="markdown-toc-4211-udp-binding">4.2.1.1 UDP Binding</a></li>
              <li><a href="#4212-tcp-binding" id="markdown-toc-4212-tcp-binding">4.2.1.2 TCP Binding</a></li>
              <li><a href="#4213-multiple-service-instances" id="markdown-toc-4213-multiple-service-instances">4.2.1.3 Multiple Service-Instances</a></li>
              <li><a href="#4214-transporting-large-someip-messages-of-udp-someip-tp" id="markdown-toc-4214-transporting-large-someip-messages-of-udp-someip-tp">4.2.1.4 Transporting large SOME/IP messages of UDP (SOME/IP-TP)</a></li>
            </ul>
          </li>
          <li><a href="#422-requestresponse-communication" id="markdown-toc-422-requestresponse-communication">4.2.2 Request/Response Communication</a></li>
          <li><a href="#423-fireforget-communication" id="markdown-toc-423-fireforget-communication">4.2.3 Fire&amp;Forget Communication</a></li>
          <li><a href="#424-notification-events" id="markdown-toc-424-notification-events">4.2.4 Notification Events</a>            <ul>
              <li><a href="#4241-strategy-for-sending-notifications" id="markdown-toc-4241-strategy-for-sending-notifications">4.2.4.1 Strategy for sending notifications</a></li>
            </ul>
          </li>
          <li><a href="#425-fields" id="markdown-toc-425-fields">4.2.5 Fields</a></li>
        </ul>
      </li>
    </ul>
  </li>
  <li><a href="#6-protocol-usage-and-guidelines" id="markdown-toc-6-protocol-usage-and-guidelines">6 Protocol usage and guidelines</a>    <ul>
      <li><a href="#61-choosing-the-transport-protocol" id="markdown-toc-61-choosing-the-transport-protocol">6.1 Choosing the transport protocol</a></li>
    </ul>
  </li>
</ul>

<h1 id="4-protocol-specification">4 Protocol Specification</h1>

<h2 id="41-specification-of-someip-message-format-serialization">4.1 Specification of SOME/IP Message Format (Serialization)</h2>

<h3 id="412-header">4.1.2 Header</h3>

<h4 id="4121-message-id-32-bit">4.1.2.1 Message ID [32 Bit]</h4>

<ul>
  <li>消息ID全局唯一，唯一的确定一个Event或者Method</li>
  <li>由两部分组成，分别占[16 bits]：
    <ul>
      <li>Service ID：所属的service ID</li>
      <li>Method ID：这里Method ID可以是服务内部的Methods或者Events的ID。名称有误导性</li>
    </ul>
  </li>
</ul>

<h4 id="4124-request-id-32-bit">4.1.2.4 Request ID [32 Bit]</h4>

<p>这个字段跟Method，field中的setter，getter(特殊的Methods)相关，因为同一个Skeleton提供的Methods可能同时并发多个请求方，这些请求方的传入参数是不同的，需要区分这些不同的请求。</p>

<blockquote>
  <p>在具体实现中，Method的处理一般是采用线程池并发处理请求</p>

</blockquote>

<p>这个ID由两部分组成，分别占[16 bits]：</p>

<ul>
  <li>Client ID [16 Bits]</li>
  <li>Session ID [16 Bits]</li>
</ul>

<p>其中，Client ID是全局唯一的。Session ID可以看作是当前Client对当前方法的请求计数器。一个Request ID完全由Proxy端产生，并且需要保证全局唯一性；Skeleton端无需关心这个ID，只需要在回复时将此ID拷贝到消息中即可，这包含了下面的结论：</p>

<ul>
  <li>Proxy端在使用一个Request ID前必须保证该ID在上一次请求中已经返回结果或者超时，或者从未被使用，否则Skeleton端返回的请求结果可能跟Proxy端的请求对不上</li>
</ul>

<blockquote>
  <p>关于Client ID： AUTOSAR文档中只介绍全局唯一，但并没有详细解释其来源。其实这是很重要的一个概念，与SOA通信的架构有关。在面向服务的通信模式中，一个服务提供方(Skeleton)有一个全局唯一的服务ID，称为Instance ID；服务ID是由Skeleton确定的。一个Skeleton可以同时提供服务给多个消费方(Proxy)，这些Proxy都需要指定其需要的具体的一个服务ID(或者所有服务ID）；当不同的Proxy所需要的服务ID是同一个时，那么对于这个服务实例来说，它就有多个Client，那么如何区分这不同的Client，AUTOSAR并没有一个Proxy服务实例的ID规定。在AUTOSAR模型系统中，唯一能确定一个Proxy的是一个全局唯一的模型元素路径，这个全局唯一的路径可以映射成全局唯一的Client ID。</p>

</blockquote>

<blockquote>
  <p>Client ID除了用在Request/Response模式中，还可以用在Event的Publish/Subscribe方式中；例如同一个Host上有多个订阅同一个外部服务ID的Client，可以只在Host上建立一条与服务提供方的通信链路，由中心节点来通过不同的Client ID是否订阅来确定是否将Event消息转发给对应的Client，因为不同Client可能订阅的Event是不同的。有了Client ID，订阅同一个服务ID的不同Clients才可能使用同一个通信链路(UDP或者TCP端口号)，否则Proxy Host这一侧无法判断是否要将一个Event消息转发给不同的Client。</p>

</blockquote>

<h4 id="4127-message-type-8-bit">4.1.2.7 Message Type [8 Bit]</h4>

<p>消息类型在SOME/IP协议中预留了位置来指定，在DDS的绑定中需要在实现时添加相关消息头信息。</p>

<ul>
  <li>所以DDS协议作为底层驱动时更加的灵活，没有SOME/IP这种协议级别的负担</li>
</ul>

<h3 id="414-serialization-of-data-structures">4.1.4 Serialization of Data Structures</h3>

<h4 id="4143-structured-datatypes-and-arguments-with-identifier-and-optional-members-tlv">4.1.4.3 Structured Datatypes and Arguments with Identifier and optional members (’TLV’)</h4>

<p>这里有个Data ID和tag的概念：</p>

<ul>
  <li>用于数据类型结构体的成员前</li>
  <li>用于Method入参前</li>
</ul>

<p>功能是为了前后的兼容性，有了Data ID，接收方可以根据已知的Data ID去选择性的处理相关的数据。Data ID的位置决定了它必须在一个结构体内部或者一个方法的所有参数范围内唯一。有了Data ID后SOME/IP还支持将数据类型信息(预先定义的枚举值）与Data ID一起序列化到消息中，称为消息的tag</p>

<blockquote>
  <p>SOME/IP相比其他通信协议，如DDS，Google Protobuf等有一个优势是支持union类型；这在车载通信与MCU交互时用的很多</p>

</blockquote>

<h2 id="42-specification-of-someip-protocol">4.2 Specification of SOME/IP Protocol</h2>

<h3 id="421-transport-protocol-bindings">4.2.1 Transport Protocol Bindings</h3>

<ul>
  <li>SOME/IP支持TCP或者UDP传输层协议</li>
  <li>如果一个Machine上同时提供多个同一个Service ID的不同Instance，则这些Instance必须被绑定到不同的Port口，无论是TCP还是UDP，详细原因分析看4.2.1.3</li>
  <li>一个Service Instance可以使用的传输层通信方式：
    <ul>
      <li>TCP单播</li>
      <li>UDP单播</li>
      <li>UDP组播</li>
    </ul>

    <blockquote>
      <p>这三个Port口是可以同时存在的，但是每一个最多只能有一个；对于服务下面的Event或者Method，只能同时对应一个TCP单播或者UDP单播，不能-》同时使用两个</p>

    </blockquote>
  </li>
</ul>

<h4 id="4211-udp-binding">4.2.1.1 UDP Binding</h4>

<ul>
  <li>Skeleton和Proxy服务实例之间只能唯一的使用一个UDP的单播通信连接进行所有的Event，Method通信</li>
</ul>

<h4 id="4212-tcp-binding">4.2.1.2 TCP Binding</h4>

<ul>
  <li>Skeleton和Proxy服务实例之间只能唯一的使用一个UDP的单播通信连接进行所有的Event，Method通信</li>
  <li>
    <p>在使用TCP传输层协议时，必须关闭Nagle算法，保证每次数据发送都必须马上打包成一个TCP segment发送出去，目的是保证消息消息实时性。</p>

    <blockquote>
      <p>Nagle算法将多个TCP发送积累成一个大的TCP segment发送的算法，目的是提高TCP数据包的payload比例；因为一个TCP报文的头部占40-Byte，如果单个segment的payload过小，则发送的数据大部分都是头部信息，降低了报文的利用率，增加了线上阻塞的风险</p>

    </blockquote>
  </li>
  <li>TCP的链接发起、重连、关闭都是由Proxy端决定的
    <ul>
      <li>当Skeleton端调用了StopOfferService接口停止服务后，不能自行终断TCP链接，而应该是Proxy端来操作，否则Proxy端会进行重连，造成资源浪费</li>
    </ul>
  </li>
</ul>

<h4 id="4213-multiple-service-instances">4.2.1.3 Multiple Service-Instances</h4>

<p>这里原文解释的已经很清楚，但由于逻辑关系的复杂性和重要性，这里有必要做一个更加详尽的解释。</p>

<ul>
  <li>首先一个Host，可以针对同一个Service提供多个不同的Service Instance，即服务的实例</li>
  <li>每一个Service Instance在一个Service ID下有一个全局唯一的Instance ID</li>
  <li>已知SOME/IP的消息头（不是SOME/IP-SD的消息头，这是服务发现的消息）是不包含Service Instance ID信息的</li>
  <li>以上就会导致一个问题：客户端(Proxy, 或者Client）如何通过SOME/IP消息头来判断这个消息来自同一个Server上的同一个Service的哪一个Instance</li>
</ul>

<p>有了问题的原因，让我们一步一步分析和解决这个问题。无论是UDP还是TCP都有一个(srcIP, srcPort, dstIP, dstPort)的tuple来表示一个通信链接：</p>

<ul>
  <li>不同Host上的服务实例，无论是同一个服务还是不同的服务，都可以通过通信链接来区分不同的Instance</li>
  <li>同一个Host上的不同的Service，可以通过其唯一的Service ID来区分</li>
  <li>同一个Host上的相同的Service，因为它们有相同的Service ID，而且SOME/IP消息头不包含Instance ID的信息，只能通过通信链接来区分不同的Instance；从一个Client Host的视角来看srcIP, dstIP是不能变动的，那么能区分的就只有srcPort或者dstPort，而Instance ID是由服务的提供方，即Skeleton侧定义的，所以区分也自然应该由Server端来区分：
    <ul>
      <li>同一个Host上相同的Service的不同Instance需要绑定到不同的Port端口</li>
    </ul>
  </li>
</ul>

<p>反之：</p>

<ul>
  <li>如果一个Client Host上有同一个服务的不同服务实例的订阅，可以通过Skeleton端不同的端口号来区分这些服务实例</li>
  <li>如果同一个Instance ID的多个Proxy，因为多个Proxy实例可以订阅同一个Instance ID提供的服务，即一发多收，则也可以通过同一个Port口来接收，因为每一个Client都有一个全局唯一的Client ID，当这个Instance ID通过唯一的通信链接到达Client Host时可以通过SOME/IP消息头部的Client ID信息来确定是由哪一个Proxy实例来接收</li>
</ul>

<blockquote>
  <p>注意：这个要求只存在于Skeleton，即服务的提供方一侧</p>

</blockquote>

<h4 id="4214-transporting-large-someip-messages-of-udp-someip-tp">4.2.1.4 Transporting large SOME/IP messages of UDP (SOME/IP-TP)</h4>

<p>针对一个UDP报文放不下的数据，SOME/IP使用SOME/IP-TP（SOME/IP Transport Protocol）来分成多个UDP报文发送，基本的原理就是每一个分割后的UDP报文都自带一个Session ID接收端通过Session ID来进行组装。SOME/IP标准的建议是能使用UDP的地方尽量使用UDP，只有在传输很大的数据，且没有严格的延迟要求的情况下才使用TCP</p>

<h3 id="422-requestresponse-communication">4.2.2 Request/Response Communication</h3>

<ul>
  <li>这里有一个要求，所有Request的参数必须按照在函数调用中的实际参数位置顺序进行序列化</li>
</ul>

<blockquote>
  <p>Request和Response在落地时，使用C++的<code class="language-plaintext highlighter-rouge">std::future/std::promise</code>来异步实现</p>

</blockquote>

<h3 id="423-fireforget-communication">4.2.3 Fire&amp;Forget Communication</h3>

<ul>
  <li>与普通Request/Response的区别在与不会等待Response</li>
</ul>

<blockquote>
  <p>这里有个很大的误区就是认为Fire&amp;Forget就是返回值为<code class="language-plaintext highlighter-rouge">void</code> 的特殊情况，其实这是错误的。当然Fire&amp;Forget返回值类型是<code class="language-plaintext highlighter-rouge">void</code>，但是与普通Request/Response返回值是<code class="language-plaintext highlighter-rouge">void</code>不同，Fire/Forget不会等待，所以无法确定，其请求的任务是否执行完毕。但是普通的返回<code class="language-plaintext highlighter-rouge">void</code>的情况下，虽然返回值是<code class="language-plaintext highlighter-rouge">void</code>，但是可以通过<code class="language-plaintext highlighter-rouge">std::future</code>的<code class="language-plaintext highlighter-rouge">get()</code>方法来判断服务方是否已经执行完毕，这种情况往往需要服务方发送完成的标志位给到请求方。而Fire&amp;Forget情况下，服务提供方是不会发送任何回复给请求方的，这也是为什么Fire&amp;Forget的请求永远不会返回错误码。</p>

</blockquote>

<h3 id="424-notification-events">4.2.4 Notification Events</h3>

<p>AUTOSAR要求当一个Proxy Host上有多个订阅同一个服务ID的Client时，实现上需要考虑降低网络流量，这其实确定了SOME/IP的实现往往需要一个中心节点。只有通过这个中心节点，做统一的收发管理，才能最大程度的优化网络流量。</p>

<h4 id="4241-strategy-for-sending-notifications">4.2.4.1 Strategy for sending notifications</h4>

<p>订阅发送的三种模式：</p>

<ul>
  <li>周期发送</li>
  <li>事件触发发送</li>
  <li>值变化超过某个范围发送</li>
</ul>

<blockquote>
  <p>实际项目中这些发送逻辑往往是用户来实现的</p>

</blockquote>

<h3 id="425-fields">4.2.5 Fields</h3>

<p>Fields是Events和Methods的融合体，具体表现在：</p>

<ul>
  <li>一个Fields需要至少包含一个<code class="language-plaintext highlighter-rouge">getter</code>，一个<code class="language-plaintext highlighter-rouge">setter</code>，一个<code class="language-plaintext highlighter-rouge">notifier</code></li>
  <li><code class="language-plaintext highlighter-rouge">getter</code>和<code class="language-plaintext highlighter-rouge">setter</code>，本质就是Methods</li>
  <li><code class="language-plaintext highlighter-rouge">notifier</code>，本质就是Event</li>
</ul>

<h1 id="6-protocol-usage-and-guidelines">6 Protocol usage and guidelines</h1>

<h2 id="61-choosing-the-transport-protocol">6.1 Choosing the transport protocol</h2>

<p>SOME/IP的传输层协议是具体到一个消息的：</p>

<ul>
  <li>每一个Event，Method等都可以配置自己的传输层协议</li>
</ul>

<p>在UDP和TCP的选择上，建议如下：</p>

<ul>
  <li>
    <blockquote>
      <p>1400Bytes，且在错误发生时没有严格实时性要求的，使用TCP</p>
    </blockquote>
  </li>
  <li>如果在错误发生时实时性要求&lt;100ms，使用UDP</li>
  <li>如果错误发生时实时性要求&lt;100ms，且消息&gt;1400 Bytes，使用SOME/IP-TP</li>
</ul>]]></content><author><name></name></author><category term="AUTOSAR" /><summary type="html"><![CDATA[SOME/IP 协议是AUTOSAR AP和CP标准中包含的面向服务的通信协议。在AP上，SOME/IP 基于网络传输层的TCP和UDP协议，属于OSI模型的应用层协议。本文不是原文的翻译，而是按照章节对重要的，且难以理解的部分进行解释，希望能帮助到有需要的读者。]]></summary></entry><entry><title type="html">SOME/IP Service Discovery解读</title><link href="http://localhost:4000/2024/04/08/SOME-IP-SD-%E8%A7%A3%E8%AF%BB.html" rel="alternate" type="text/html" title="SOME/IP Service Discovery解读" /><published>2024-04-08T19:22:46+08:00</published><updated>2024-04-08T19:22:46+08:00</updated><id>http://localhost:4000/2024/04/08/SOME-IP-SD-%E8%A7%A3%E8%AF%BB</id><content type="html" xml:base="http://localhost:4000/2024/04/08/SOME-IP-SD-%E8%A7%A3%E8%AF%BB.html"><![CDATA[<p><a href="https://www.autosar.org/fileadmin/standards/R22-11/FO/AUTOSAR_PRS_SOMEIPServiceDiscoveryProtocol.pdf">SOME/IP SD</a>是AUTOSAR规范中定义的用于SOME/IP协议运行时服务发现的协议。它运行于SOME/IP协议之上，可以认为是基于SOME/IP协议的应用层协议。</p>

<blockquote>
  <p>AUTOSAR模型中也定义了SOME/IP纯静态的发现机制，是通过服务实例到Machine的映射完成的。可以参见<a href="https://www.autosar.org/fileadmin/standards/R22-11/AP/AUTOSAR_TPS_ManifestSpecification.pdf">Specification Manifest</a>第11.3.1.3章节。当使用纯静态的发现机制后，SOME/IP SD可以不使用。</p>

</blockquote>

<p>本文不是SOME/IP SD协议的翻译，而是按照原文章节对各个章节中的难以理解的部分进行解读。限于个人理解，如有错误，请以原文为准。</p>

<ul id="markdown-toc">
  <li><a href="#1-introduction-and-overview" id="markdown-toc-1-introduction-and-overview">1 Introduction and overview</a>    <ul>
      <li><a href="#13-dependencies" id="markdown-toc-13-dependencies">1.3 Dependencies</a>        <ul>
          <li><a href="#131-dependencies-to-other-protocol-layers" id="markdown-toc-131-dependencies-to-other-protocol-layers">1.3.1 Dependencies to other protocol layers</a></li>
        </ul>
      </li>
    </ul>
  </li>
  <li><a href="#5-protocol-specification" id="markdown-toc-5-protocol-specification">5 Protocol specification</a>    <ul>
      <li><a href="#51-someip-service-discovery-someip-sd" id="markdown-toc-51-someip-service-discovery-someip-sd">5.1 SOME/IP Service Discovery (SOME/IP-SD)</a>        <ul>
          <li><a href="#511-general" id="markdown-toc-511-general">5.1.1 General</a></li>
          <li><a href="#512-someip-sd-message-format" id="markdown-toc-512-someip-sd-message-format">5.1.2 SOME/IP-SD Message Format</a>            <ul>
              <li><a href="#5121-general-requirements" id="markdown-toc-5121-general-requirements">5.1.2.1 General Requirements</a></li>
              <li><a href="#5122-someip-sd-header" id="markdown-toc-5122-someip-sd-header">5.1.2.2 SOME/IP-SD Header</a></li>
              <li><a href="#5123-entry-format" id="markdown-toc-5123-entry-format">5.1.2.3 Entry Format</a></li>
              <li><a href="#5124-options-format" id="markdown-toc-5124-options-format">5.1.2.4 Options Format</a></li>
              <li><a href="#5125-service-entries" id="markdown-toc-5125-service-entries">5.1.2.5 Service Entries</a>                <ul>
                  <li><a href="#51251-find-service-entry" id="markdown-toc-51251-find-service-entry">5.1.2.5.1 Find Service Entry</a></li>
                  <li><a href="#51252-offer-service-entry" id="markdown-toc-51252-offer-service-entry">5.1.2.5.2 Offer Service Entry</a></li>
                  <li><a href="#51253-stop-offer-service-entry" id="markdown-toc-51253-stop-offer-service-entry">5.1.2.5.3 Stop Offer Service Entry</a></li>
                </ul>
              </li>
              <li><a href="#5126-endpoint-handling-for-services-and-events" id="markdown-toc-5126-endpoint-handling-for-services-and-events">5.1.2.6 Endpoint Handling for Services and Events</a></li>
            </ul>
          </li>
          <li><a href="#513-service-discovery-messages" id="markdown-toc-513-service-discovery-messages">5.1.3 Service Discovery Messages</a>            <ul>
              <li><a href="#5131-eventgroup-entry" id="markdown-toc-5131-eventgroup-entry">5.1.3.1 Eventgroup Entry</a></li>
            </ul>
          </li>
          <li><a href="#514-service-discovery-communication-behavior" id="markdown-toc-514-service-discovery-communication-behavior">5.1.4 Service Discovery Communication Behavior</a>            <ul>
              <li><a href="#5141-startup-behavior" id="markdown-toc-5141-startup-behavior">5.1.4.1 Startup Behavior</a></li>
              <li><a href="#5144-state-machines" id="markdown-toc-5144-state-machines">5.1.4.4 State Machines</a></li>
            </ul>
          </li>
          <li><a href="#516-publishsubscribe-with-someip-and-someip-sd" id="markdown-toc-516-publishsubscribe-with-someip-and-someip-sd">5.1.6 Publish/Subscribe with SOME/IP and SOME/IP-SD</a></li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<h1 id="1-introduction-and-overview">1 Introduction and overview</h1>

<h2 id="13-dependencies">1.3 Dependencies</h2>

<h3 id="131-dependencies-to-other-protocol-layers">1.3.1 Dependencies to other protocol layers</h3>

<p>SOME/IP-SD是基于SOME/IP协议，且底层的传输层协议使用UDP</p>

<h1 id="5-protocol-specification">5 Protocol specification</h1>

<h2 id="51-someip-service-discovery-someip-sd">5.1 SOME/IP Service Discovery (SOME/IP-SD)</h2>

<h3 id="511-general">5.1.1 General</h3>

<p>SOME/IP-SD完成三个方面的功能：</p>

<ul>
  <li>确定服务实例的位置</li>
  <li>确定服务实例的状态</li>
  <li>发布/订阅处理</li>
</ul>

<p>服务实例的位置一般都是由静态的配置文件定义的，运行时只需要解析相关配置文件即可；确定服务实例的状态才是服务发现的重要步骤。</p>

<h3 id="512-someip-sd-message-format">5.1.2 SOME/IP-SD Message Format</h3>

<p>了解了SOME/IP-SD的消息结构，也就理解了SOME/IP-SD的服务发现协议。可以看出SOME/IP-SD是一个普通SOME/IP消息的payload，也就是说SD是基于SOME/IP协议进行的。可以将SOME/IP-SD协议理解为基于SOME/IP协议之上的一个应用层协议。</p>

<p><img src="/assets/images/some_ip_sd_pdu.jpg" alt="Untitled" /></p>

<h4 id="5121-general-requirements">5.1.2.1 General Requirements</h4>

<p>首先SOME/IP-SD的消息只是一个特殊的SOME/IP消息，格式需要符合SOME/IP消息头的布局，与普通SOME/IP消息的区别在与其内容：</p>

<ul>
  <li>Service-ID是固定值0xFFFF</li>
  <li>Method-ID是固定值0x8100</li>
  <li>Client-ID是固定值0x0000</li>
</ul>

<p>其他字段，例如版本信息，消息类型，返回码等都是固定值。</p>

<h4 id="5122-someip-sd-header">5.1.2.2 SOME/IP-SD Header</h4>

<p>SOME/IP-SD整个是SOME/IP消息的payload，这个payload有自己的协议规范，这里介绍它的头部信息。</p>

<ul>
  <li>前8bits称为<code class="language-plaintext highlighter-rouge">flags</code>，其中第一个bit为1表示Reboot；当Host启动后开始发送的所有SOME/IP-SD消息都将这个bit设为1，表示Host正在启动；当session ID自增超过16 bits的最大值，重新从1开始自增时，将这个bit设置为0，表示Host启动完成</li>
  <li>对于发送方：这个重启bit和session的counter是针对每一个多播和单播</li>
  <li>针对接收方：这个重启bit和session counter是针对每一个[srcIP, srcPort, dstIP, dstPort]通信链接</li>
</ul>

<p>SOME/IP-SD通过如下条件判断消息的发送方是否重启：</p>

<ul>
  <li>if old.reboot==0 and new.reboot==1</li>
  <li>if old.reboot==1 and new.reboot==1 and old.session_id&gt;=new.session_id</li>
</ul>

<h4 id="5123-entry-format">5.1.2.3 Entry Format</h4>

<p>Entry类型：</p>

<hr />

<p>这一部分是服务发现的实际内容信息：</p>

<ul>
  <li>一个SOME/IP-SD消息可以包含多个服务实例的信息(Entry)</li>
  <li>实际发现内容分为两类：
    <ul>
      <li>服务发现类型：包含当前Host提供的Service ID， Instance ID， 版本信息等</li>
    </ul>

    <p><img src="/assets/images/some_ip_service_enty_type.jpg" alt="Untitled" /></p>

    <blockquote>
      <p>注意这里包含Instance ID信息，但是是作为SOME/IP的payload，不是SOME/IP头部的一部分</p>

      <ul>
        <li>EventGroup类型：除了包含服务发现类型以外，最后包含了EventGroupID信息</li>
      </ul>
    </blockquote>

    <p><img src="/assets/images/some_ip_event_group_entry_type.jpg" alt="Untitled" /></p>
  </li>
</ul>

<p>这两种Entry的内容很多都是重复的，有些就是字面意思，不再解释，其他分别解释如下：</p>

<p>Type:</p>

<hr />

<p>这两类信息消息类型的第一个部分都是Type，这是每条Entry都包含的信息，表示了当前条目是需要发现的、对外提供的、还是停止提供的。对于Service Entry有如下选项：</p>

<ul>
  <li>FindService (0x00)</li>
  <li>OfferService (0x01)</li>
  <li>StopOfferService (0x01)</li>
</ul>

<p>对于EventGroupEntry，有如下选项：</p>

<ul>
  <li>SubscribeEventGroup (0x06)</li>
  <li>StopSubscribeEventgroup (0x06)</li>
  <li>SubscribeEventGroupAck (0x07)</li>
  <li>SubscribeEventgroupNack (0x07)</li>
</ul>

<blockquote>
  <p>ID来源于文档，但是待确认。AUTOSAR官方文档有很多笔误</p>

</blockquote>

<p>Option位置：</p>

<hr />

<p>每个条目有了以上这些信息后，就需要知道他们更进一步的信息了，比如IP地址，Port端口，传输层协议等，这些最终需要传达的信息。SOME/IP-SD中这些信息是通过Options来包含的。每个条目有两段，每段有连续的多个Options组成，原因如下：</p>

<ul>
  <li>第一段中的Options是所有SOME/IP-SD共有的Options</li>
  <li>第二段是当前SOME/IP-SD独有的Options</li>
</ul>

<p>在每个Entry的头部消息中有如下字段引导至第一段Options和第二段Options：[Index 1st Options, Index 2nd Options, number of 1st Options, number of 2nd Options]， 这样做的目的是最快的找到Option所在的位置.</p>

<h4 id="5124-options-format">5.1.2.4 Options Format</h4>

<p>通过以上层层递进，现在已经到达最终的目的地了：具体的Option内容信息。它也有自己的格式规范，每一个Option都必须以如下格式开头：</p>

<p><img src="/assets/images/some_ip_option_header.jpg" alt="Untitled" /></p>

<p>Configuration Option（Type 0x01）</p>

<hr />

<p>简单理解为一组键：值对，且符合<a href="https://www.cloudflare.com/zh-cn/learning/dns/dns-records/dns-txt-record/">DNS TXT</a>和<a href="http://www.dns-sd.org/">DNS SD</a>记录格式</p>

<p><img src="/assets/images/some_ip_configuration_option.jpg" alt="Untitled" /></p>

<p>Load Balancing Option（Type 0x02）</p>

<hr />

<p>这个选项在OfferService 时有用，目的是为每一个Instance提供优先级，proxy端可以根据这个选项提供的信息选择Instance；信息格式参考<a href="https://www.cloudflare.com/zh-cn/learning/dns/dns-records/dns-srv-record/">DNS SRV</a>。这里如果一个proxy想发现一个服务ID下所有的实例，Service Instance 设置为 0xFFFF，当这个proxy得到所有的实例后，应当使用优先级最高的那个实例来构建proxy实例</p>

<p><img src="/assets/images/some_ip_load_balancing_option.jpg" alt="Untitled" /></p>

<p>IPv4 Endpoint Option（Type 0x04）</p>

<hr />

<p>IPv4选项用于指定当前Entry的IPv4地址信息，包括IP地址，传输层协议，Port端口号。注意这是一个skeleton和proxy都可能需要的选项：</p>

<ul>
  <li>对于skeleton，在OfferService Entry中提供Instance的源地址信息
    <ul>
      <li>一个Instance可以在TCP和UDP上分别占用一个Port口，且至少有一个。也就是说，一个服务实例可以同时在TCP和UDP传输协议上提供服务</li>
      <li>Instance内部的元素，例如Event，Method必须指定是在TCP还是在UDP上通信，也就是说，服务内具体的Event，Method的通信都是唯一确定的
        <blockquote>
          <p>Event, Method等使用UDP还是TCP是在Deployment模型中确定的，Server和Client端在服务发现前已经知道</p>
        </blockquote>
      </li>
      <li>一个Host上相同Service的不同Instance必须使用不同的TCP和UDP的Port；不同的Service的Instance可以使用同一个TCP和UDP的Port</li>
    </ul>
  </li>
  <li>对于proxy，在SubscribeEventGroup Entry中使用这个选项来传递proxy实例已经准备好的，用于接收Events的IPv4链接信息
    <ul>
      <li>Client端可以有0~2个单播EndPoint，如果是2个，则TCP和UDP各一个</li>
      <li>Client端还有至多1个组播接收地址</li>
    </ul>
  </li>
</ul>

<blockquote>
  <p>当proxy对外提供准备好接收的EventGroup信息时，所有通信资源例如socket等都已准备完毕</p>
</blockquote>

<p><img src="/assets/images/some_ip_endpoint_option.jpg" alt="Untitled" /></p>

<p>IPv4 Multicast Option（Type 0x14）</p>

<hr />

<p>该选项可以被skeleton或者proxy端组播发送：</p>

<ul>
  <li>skeleton组播发送自己提供Instance的组播地址，端口号</li>
  <li>proxy组播发送自己接收服务的组播地址，端口号</li>
</ul>

<p><img src="/assets/images/some_ip_multicast_option.jpg" alt="Untitled" /></p>

<p>关于组播，只能在Entry类型为 SubscribeEventgroup，StopSubscribeEventgroup，SubscribeEventgroupAck中被使用，引用一次或者零次：</p>

<ul>
  <li>当被SubscribeEventgroup引用，表示Client端接收组播Events的地址</li>
  <li>当被StopSubscribeEventgroup引用，表示Client端停止接收组播Events的地址</li>
  <li>当被SubscribeEventgroupAck引用，表示Server端发送组播Events的地址</li>
</ul>

<blockquote>
  <p>要理解组播在EventGroup的角色，首先要理解是EventGroup分为三种：单播EventGroup，即所有Events都是通过一个单播通信链接（EndPoint pair）发送；组播EventGroup，即所有Events都被发送到一个组播EndPoint上，注意这是从Server的角度来说的，如果Server端通过单播提供服务，但是Client端在SubscribeEventGroup时提供了组播的地址，Server也会通过该组播地址发送给改Client；自适应EventGroup，即其中的Events的发送方式是根据运行时订阅状态在组播和单播之间切换的，这点在后面会详细解释。所以，无论是SubscribeEventgroup，还是SubscribeEventgroupAck引用的组播地址，都是Server或者Client指明自己发送或者接收这个EventGroup的EndPoint</p>

</blockquote>

<h4 id="5125-service-entries">5.1.2.5 Service Entries</h4>

<h5 id="51251-find-service-entry">5.1.2.5.1 Find Service Entry</h5>

<ul>
  <li>在FindServiceEntry中可以选择发现所有当前服务ID下的实例，也可以具体制定实例ID</li>
  <li>同样也可以发现所有版本的服务，和具体指定某个版本信息</li>
</ul>

<blockquote>
  <p>FindService Entry中不包含通信EndPoint的Option；如果包含了，接收方应当忽略这些信息</p>

</blockquote>

<h5 id="51252-offer-service-entry">5.1.2.5.2 Offer Service Entry</h5>

<p>与FindServiceEntry不同，OfferServiceEntry需要：</p>

<ul>
  <li>写明服务ID，实例ID，版本信息</li>
  <li>如果支持IPv4/IPv6传输层协议，则必须在Option中包含具体的通信EndPoint信息</li>
</ul>

<h5 id="51253-stop-offer-service-entry">5.1.2.5.3 Stop Offer Service Entry</h5>

<p>除了Entry的Type和TTL等信息不同外，与StartOfferService所包含的信息相同</p>

<p>各种Type的Entry的option使用矩阵表格：</p>

<p><img src="/assets/images/some_ip_sd_allowed_entry.jpg" alt="Untitled" /></p>

<blockquote>
  <p>SubscribeEventgroup和StopSubscribeEventgroup至少需要指定一个Unicast或者Multicast EndPoint</p>

</blockquote>

<h4 id="5126-endpoint-handling-for-services-and-events">5.1.2.6 Endpoint Handling for Services and Events</h4>

<p>这里有一组很重要的概念需要解释和澄清：</p>

<ul>
  <li>一个Service Instance的OfferService Entry信息中可以包含至多两个，至少一个通信EndPoint信息
    <ul>
      <li>如果是两个则必须一个是TCP，一个是UDP</li>
    </ul>
  </li>
  <li>Service中不同的Event，Method可以使用这两个EndPoint中的一个作为底层通信通道，但是只能选择其中一个
    <ul>
      <li>一个Event和Method不能同时在两个EndPoint发送</li>
    </ul>
  </li>
  <li>每个Event，Method等的通信EndPoint信息都是静态配置的，例如在json配置文件中</li>
</ul>

<p>一个完整的服务发现和订阅流程图：</p>

<p><img src="/assets/images/some_ip_sd_flow.jpg" alt="Untitled" /></p>

<p>这个流程中有如下需要说明：</p>

<ul>
  <li>OfferService必须使用单播地址，无论是TCP还是UDP</li>
  <li>如果服务以TCP方式提供，Client端在收到OfferService的SOME/IP-SD信息后负责发起TCP链接</li>
  <li>在SubscribeEventGroup时，Client端会把上一步创建的TCP链接信息以及UDP信息发给Server</li>
  <li>Server主动发送第一个初始Events消息</li>
  <li>如果是组播EventGroup或者是自适应EventGroup，Server也会通过SubscribeEventgroupAck返回通过组播发送的Events的组播地址，Client端要尽快开启监听这个端口，避免错过这些通过组播发送的Events</li>
</ul>

<blockquote>
  <p>TCP的Event可以在SubscribeEventGroup发送前就已经开始发送；且Initial Event必须从Server到Client以Unicast的通信发送</p>

</blockquote>

<h3 id="513-service-discovery-messages">5.1.3 Service Discovery Messages</h3>

<ul>
  <li>所有SD的消息，无论单播还是组播，都需要发送到<code class="language-plaintext highlighter-rouge">SD_PORT</code></li>
  <li>所有组播的SD消息，都需要使用<code class="language-plaintext highlighter-rouge">SD_MULTICAST_IP</code></li>
</ul>

<h4 id="5131-eventgroup-entry">5.1.3.1 Eventgroup Entry</h4>

<p>SubscribeEventgroup Entry</p>

<hr />

<p>它可以引用Client端的EndPoint，用于指定Client端接收Events的通信端口信息：</p>

<ul>
  <li>可以引用至多两个单播地址，至多一个组播地址</li>
  <li>如果是单播地址，要么是IPv4，要么是IPv6</li>
  <li>单播地址TCP和UDP可以分别有一个EndPoint</li>
  <li>组播地址要么是IPv4，或者IPv6</li>
</ul>

<blockquote>
  <p>SubscribeEventGroup中单播和组播是二选一的关系；单播可以有UDP和TCP两个EndPoint</p>

</blockquote>

<p>SubscribeEventgroupAck Entry</p>

<hr />

<p>如果组播EventGroup或者自适应EventGroup, 在这个Ack中应该引用一个组播地址：</p>

<ul>
  <li>该组播地址可以是IPv4或者IPv6，且只能有一个</li>
</ul>

<blockquote>
  <p>单播EventGroup，发送和接收地址是Client和Server握手决定的；对于组播EventGroup或自适应EventGroup，如果是Server仅支持组播发送，则通过SubscribeEventGroupAck通知Client，如果是Client仅支持组播接收则通过SubscribeEventGroup通知Server</p>

</blockquote>

<blockquote>
  <p>如果Server发送Initial Events，则SubscribeEventGroup必须指定一个单播地址，因为Initial Events必须以单播形式发送。如果Client以组播形式订阅EventGroup，Server怎么以单播形式向Client发送？这一段解释原文编排错误，需要后续考证</p>

</blockquote>

<h3 id="514-service-discovery-communication-behavior">5.1.4 Service Discovery Communication Behavior</h3>

<p>SOME/IP-SD应尽量将Entry放在一个SOME/IP 消息中：</p>

<ul>
  <li>不同服务Ins’tan’ce的Entry可以放在一个SD消息中</li>
  <li>相同服务Instance的不同Entry可以放在一个SD消息中</li>
</ul>

<h4 id="5141-startup-behavior">5.1.4.1 Startup Behavior</h4>

<p>无论是Server的Instance还是Client端的实例，在启动阶段都需要经过如下三个阶段：</p>

<ul>
  <li>Initial Wait Phase
    <ul>
      <li>Client端进入该状态的条件是：UDP广播端口资源已经就绪且应用调用了FindService</li>
      <li>Server端进入该状态的条件是：UDP广播端口资源已经就绪且对外提供服务的条件已经具备</li>
    </ul>
  </li>
  <li>Repetition Phase</li>
  <li>Main Phase</li>
</ul>

<blockquote>
  <p>虽然阶段名称相同，但是Server端和Client端在每个阶段的动作是不同的；在具体实现中状态机不仅会有这三个阶段，还会有其他状态</p>

</blockquote>

<h4 id="5144-state-machines">5.1.4.4 State Machines</h4>

<p>Server的状态机</p>

<hr />

<p><img src="/assets/images/some_ip_server_statemachine.jpg" alt="Untitled" /></p>

<p>说明如下：</p>

<ul>
  <li>状态机进入Initial Wait Phase阶段后需要在<code class="language-plaintext highlighter-rouge">INITIAL_DELAY_MIN</code>和<code class="language-plaintext highlighter-rouge">INITIAL_DELAY_MAX</code>区间随机的等待一段时间，等待完成后直接进入下一个阶段</li>
  <li>在Repetition Phase阶段，从<code class="language-plaintext highlighter-rouge">REPETITION_BASE_DELAY</code>间隔值开始，每发送一次，间隔时间扩大一倍，直到发送次数达到<code class="language-plaintext highlighter-rouge">REPETITION_MAX</code>次，进入下一个阶段</li>
  <li>在Maint Phase，按照<code class="language-plaintext highlighter-rouge">CYCLIC_ANNOUNCE_DELAY</code>周期性的间隔发送</li>
  <li>在Repetition Phase和Main Phase阶段，如果收到FindService请求，等待一个在<code class="language-plaintext highlighter-rouge">RequestResponseMaxDelay</code>和<code class="language-plaintext highlighter-rouge">RequestResponseMinDelay</code>的随机值后发送一次</li>
</ul>

<blockquote>
  <p>Server侧的服务发现信息一直是周期性发送的</p>

</blockquote>

<p>Client端的状态机</p>

<hr />

<p><img src="/assets/images/some_ip_client_statemachine.jpg" alt="Untitled" /></p>

<p>说明如下：</p>

<ul>
  <li>同样有三个阶段，但是与sever端不同，FindService不会一直发送，而是通过收到的OfferService中的TTL来更新当前已发现的服务的有效性</li>
  <li>如果有效性失效，或者需要发现新的服务，才会调用FindService</li>
  <li>调用FindService的，而不是默认等待下一个OfferService的原因是，在Server端一旦受到FindService就会触发一次OfferService的发送，而不是要等到下一个发送周期</li>
</ul>

<blockquote>
  <p>SOME/IP-SD的服务发现是一种需要一直刷新来保持发现信息有效性的协议，这个刷新是由Server端的OfferService来负责主动发送的</p>

</blockquote>

<h3 id="516-publishsubscribe-with-someip-and-someip-sd">5.1.6 Publish/Subscribe with SOME/IP and SOME/IP-SD</h3>

<ul>
  <li>如果Client端订阅了多个EventGroup，这些EventGroup包含了相同的Events，那么这些Events不会被重复发送，但是这个要求不适用于Initial Events，即第一次初始发送</li>
</ul>

<p>SOME/IP SD发布订阅流程图如下：</p>

<p><img src="/assets/images/some_ip_pub_sub_flow.jpg" alt="Untitled" /></p>

<p>单播EventGroup Server端状态机</p>

<hr />

<p><img src="/assets/images/some_ip_state_unicast.jpg" alt="Untitled" /></p>

<p>组播EventGroup Server端状态机</p>

<hr />

<p><img src="/assets/images/some_ip_multicast_option.jpg" alt="Untitled" /></p>

<ul>
  <li>单播和组播状态机最重要的区别是组播需要维护订阅数量，当订阅数为零时停止发送</li>
</ul>

<p>自适应单播/组播EventGroup</p>

<hr />

<p><img src="/assets/images/some_ip_state_adaptive.jpg" alt="Untitled" /></p>

<p>对于配置为UDP协议的Event，SOME/IP支持在到达单播使用上限时自动在单播和组播之间切换；切换逻辑通过<code class="language-plaintext highlighter-rouge">MULTICAST_THRESHOLD</code>来控制，具体的切换逻辑用伪代码表示为：</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">n</span> <span class="o">=</span> <span class="n">MULTICAST_THRESHOLD</span>
<span class="n">subCnt</span> <span class="o">=</span> <span class="n">subscribe</span> <span class="n">counter</span>
<span class="n">unicastLimit</span> <span class="o">=</span> <span class="nb">max</span> <span class="n">number</span> <span class="n">of</span> <span class="n">subscribtion</span> <span class="n">to</span> <span class="n">switch</span> <span class="k">from</span> <span class="n">unicast</span> <span class="n">to</span> <span class="n">multicast</span>
<span class="n">when</span> <span class="n">new</span> <span class="n">subCnt</span><span class="p">:</span>
	<span class="k">if</span> <span class="n">n</span> <span class="o">==</span><span class="mi">0</span><span class="p">:</span>
		<span class="n">unicast</span> <span class="ow">or</span> <span class="n">multicast</span> <span class="n">depends</span> <span class="n">on</span> <span class="n">Client</span> <span class="n">Subscribe</span> <span class="n">info</span>
	<span class="k">elif</span> <span class="n">n</span><span class="o">==</span><span class="mi">1</span><span class="p">:</span>
		<span class="n">multicast</span> <span class="n">only</span> <span class="n">to</span> <span class="n">Server</span> <span class="n">multicast</span> <span class="n">EndPoint</span>
	<span class="k">elif</span> <span class="n">n</span><span class="o">&gt;</span><span class="mi">1</span> <span class="ow">and</span> <span class="n">subCnt</span> <span class="o">&gt;=</span> <span class="n">unicastLimit</span><span class="p">:</span>
		<span class="n">switch</span> <span class="n">to</span> <span class="n">Server</span> <span class="n">multicast</span> <span class="n">EndPoint</span>
	<span class="k">elif</span> <span class="n">n</span><span class="o">&gt;</span><span class="mi">1</span> <span class="ow">and</span> <span class="n">subCnt</span> <span class="o">&lt;</span> <span class="n">unicastLimit</span><span class="p">:</span>
		<span class="n">unicast</span> <span class="ow">or</span> <span class="n">multicast</span> <span class="n">depends</span> <span class="n">on</span> <span class="n">Client</span> <span class="n">Subscribe</span> <span class="n">info</span>
</code></pre></div></div>

<blockquote>
  <p>SOME/IP-SD服务发现提供在没有静态配置的情况下在运行时发现所需要的服务以及其EndPoint信息；但是它可以被纯静态的配置替代，比如可以使用配置文件静态的将所有的通信配置，EndPoint信息定义好，只需要在运行时解析配置文件获取相关通信配置即可，这些都不是SOME/IP-SD协议的一部分。</p>
</blockquote>]]></content><author><name></name></author><category term="AUTOSAR" /><summary type="html"><![CDATA[SOME/IP SD是AUTOSAR规范中定义的用于SOME/IP协议运行时服务发现的协议。它运行于SOME/IP协议之上，可以认为是基于SOME/IP协议的应用层协议。]]></summary></entry><entry><title type="html">C++ Parameter Binding</title><link href="http://localhost:4000/2024/04/05/C++-Parameter-Binding.html" rel="alternate" type="text/html" title="C++ Parameter Binding" /><published>2024-04-05T19:22:46+08:00</published><updated>2024-04-05T19:22:46+08:00</updated><id>http://localhost:4000/2024/04/05/C++-Parameter-Binding</id><content type="html" xml:base="http://localhost:4000/2024/04/05/C++-Parameter-Binding.html"><![CDATA[<ul id="markdown-toc">
  <li><a href="#绑定矩阵binding-matrix" id="markdown-toc-绑定矩阵binding-matrix">绑定矩阵（binding matrix）</a></li>
  <li><a href="#示例代码" id="markdown-toc-示例代码">示例代码</a></li>
</ul>

<h1 id="绑定矩阵binding-matrix">绑定矩阵（binding matrix）</h1>

<p>假设：</p>
<ul>
  <li>类型<code class="language-plaintext highlighter-rouge">T</code>为定义了所有特殊成员函数的的类型</li>
  <li>表格左侧为形参类型</li>
  <li>表格上方为实参类型</li>
</ul>

<p>则C++有如下的绑定矩阵，且绑定时的动作如下：</p>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>T(lvalue)</th>
      <th>T(rvalue)</th>
      <th>T&amp;</th>
      <th>T&amp;&amp;</th>
      <th>const T(lvalue)</th>
      <th>const T(rvalue)</th>
      <th>const T&amp;</th>
      <th>const T&amp;&amp;</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>T</td>
      <td>拷贝构造</td>
      <td>移动构造</td>
      <td>拷贝构造</td>
      <td>移动构造</td>
      <td>移动构造</td>
      <td>拷贝构造</td>
      <td>拷贝构造</td>
      <td>拷贝构造</td>
    </tr>
    <tr>
      <td>T&amp;</td>
      <td>引用绑定</td>
      <td>No</td>
      <td>引用绑定</td>
      <td>引用绑定</td>
      <td>No</td>
      <td>No</td>
      <td>No</td>
      <td>No</td>
    </tr>
    <tr>
      <td>T&amp;&amp;</td>
      <td>No</td>
      <td>引用绑定</td>
      <td>No</td>
      <td>No(Note3)</td>
      <td>No</td>
      <td>No</td>
      <td>No</td>
      <td>No(Note3)</td>
    </tr>
    <tr>
      <td>const T</td>
      <td>拷贝构造</td>
      <td>移动构造</td>
      <td>拷贝构造</td>
      <td>移动构造</td>
      <td>拷贝构造</td>
      <td>移动构造</td>
      <td>拷贝构造</td>
      <td>移动构造</td>
    </tr>
    <tr>
      <td>const T&amp;</td>
      <td>引用绑定</td>
      <td>引用绑定</td>
      <td>引用绑定</td>
      <td>引用绑定</td>
      <td>引用绑定</td>
      <td>引用绑定</td>
      <td>引用绑定</td>
      <td>引用绑定</td>
    </tr>
    <tr>
      <td>const T&amp;&amp;</td>
      <td>No</td>
      <td>引用绑定</td>
      <td>No</td>
      <td>No(Note3)</td>
      <td>No</td>
      <td>引用绑定</td>
      <td>No</td>
      <td>No(Note3)</td>
    </tr>
  </tbody>
</table>

<p>说明：</p>
<ol>
  <li>当传入右值构造一个对象时，如果没有定义移动构造函数，则会默认调用拷贝构造函数</li>
  <li>以上表格中默认T类型定义了所有的类特殊成员函数</li>
  <li>所有有名的变量，包括右值引用都是左值，所以不能传递给接收右值引用的入参类型！！</li>
  <li>所有引用都是有名称的左值</li>
  <li>函数调用参数传递分两步：
    <ol>
      <li>形参要通过binding绑定实参</li>
      <li>无论函数形参是哪种类型，在函数内部，形参都被作为左值使用，如果要保留形参类型，必须使用std::forward转发</li>
    </ol>
  </li>
</ol>

<h1 id="示例代码">示例代码</h1>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;utility&gt;</span><span class="cp">
</span>
<span class="k">struct</span> <span class="nc">Example</span> <span class="p">{</span>
  <span class="c1">// constructor</span>
  <span class="n">Example</span><span class="p">()</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Example constructor"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>
  <span class="c1">// destructor</span>
  <span class="o">~</span><span class="n">Example</span><span class="p">()</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Example destructor"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>
  <span class="c1">// copy constructor</span>
  <span class="n">Example</span><span class="p">(</span><span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Example copy constructor"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
  <span class="p">}</span>
  <span class="c1">// copy assignment operator</span>
  <span class="n">Example</span> <span class="o">&amp;</span><span class="k">operator</span><span class="o">=</span><span class="p">(</span><span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Example copy assignment operator"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="k">return</span> <span class="o">*</span><span class="k">this</span><span class="p">;</span>
  <span class="p">}</span>
  <span class="c1">// move constructor</span>
  <span class="n">Example</span><span class="p">(</span><span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e</span><span class="p">)</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Example move constructor"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>
  <span class="c1">// move assignment operator</span>
  <span class="n">Example</span> <span class="o">&amp;</span><span class="k">operator</span><span class="o">=</span><span class="p">(</span><span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Example move assignment operator"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="k">return</span> <span class="o">*</span><span class="k">this</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">};</span>

<span class="c1">// write a function, that takes a parameter by value</span>
<span class="kt">void</span> <span class="nf">func</span><span class="p">(</span><span class="n">Example</span> <span class="n">e</span><span class="p">)</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>
<span class="c1">// write a function, that takes a parameter by lvalue reference</span>
<span class="kt">void</span> <span class="nf">func_lref</span><span class="p">(</span><span class="n">Example</span> <span class="o">&amp;</span><span class="n">e</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func lvalue reference"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
<span class="p">}</span>
<span class="c1">// write a function, that takes a parameter by rvalue reference</span>
<span class="kt">void</span> <span class="nf">func_rref</span><span class="p">(</span><span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func rvalue reference"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
<span class="p">}</span>
<span class="c1">// function that takes a const value</span>
<span class="kt">void</span> <span class="nf">func_const</span><span class="p">(</span><span class="k">const</span> <span class="n">Example</span> <span class="n">e</span><span class="p">)</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func const"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>
<span class="c1">// function that takes a const lvalue reference</span>
<span class="kt">void</span> <span class="nf">func_const_lref</span><span class="p">(</span><span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func const lvalue reference"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
<span class="p">}</span>
<span class="c1">// function that takes a const rvalue reference</span>
<span class="kt">void</span> <span class="nf">func_const_rref</span><span class="p">(</span><span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func const rvalue reference"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">// test func, to pass various types of objects into it</span>
<span class="kt">void</span> <span class="nf">test_func</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">Example</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">func</span><span class="p">(</span><span class="n">e</span><span class="p">);</span>         <span class="c1">// lvalue</span>
  <span class="n">func</span><span class="p">(</span><span class="n">Example</span><span class="p">());</span> <span class="c1">// rvalue</span>
  <span class="n">func</span><span class="p">(</span><span class="n">e_ref</span><span class="p">);</span>     <span class="c1">// lvalue reference</span>
  <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="n">func</span><span class="p">(</span><span class="n">e_rref</span><span class="p">);</span> <span class="c1">// rvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="n">e_const</span><span class="p">;</span>
  <span class="n">func</span><span class="p">(</span><span class="n">e_const</span><span class="p">);</span>                               <span class="c1">// const lvalue</span>
  <span class="n">func</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="k">const</span> <span class="n">Example</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Example</span><span class="p">()));</span> <span class="c1">// const rvalue</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_const_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">func</span><span class="p">(</span><span class="n">e_const_ref</span><span class="p">);</span> <span class="c1">// const lvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_const_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="n">func</span><span class="p">(</span><span class="n">e_const_rref</span><span class="p">);</span> <span class="c1">// const rvalue reference</span>
<span class="p">}</span>

<span class="c1">// test func_lref, to pass various types of objects into it</span>
<span class="kt">void</span> <span class="nf">test_func_lref</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">Example</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">func_lref</span><span class="p">(</span><span class="n">e</span><span class="p">);</span> <span class="c1">// lvalue</span>
  <span class="c1">// func_lref(Example()); // rvalue</span>
  <span class="n">func_lref</span><span class="p">(</span><span class="n">e_ref</span><span class="p">);</span> <span class="c1">// lvalue reference</span>
  <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="n">func_lref</span><span class="p">(</span><span class="n">e_rref</span><span class="p">);</span> <span class="c1">// rvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="n">e_const</span><span class="p">;</span>
  <span class="c1">// func_lref(e_const);                               // const lvalue</span>
  <span class="c1">// func_lref(static_cast&lt;const Example&gt;(Example())); // const rvalue</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_const_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="c1">// func_lref(e_const_ref); // const lvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_const_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="c1">// func_lref(e_const_rref); // const rvalue reference</span>
<span class="p">}</span>

<span class="c1">// test func_rref, to pass various types of objects into it</span>
<span class="kt">void</span> <span class="nf">test_func_rref</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">Example</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="c1">// func_rref(e);         // lvalue</span>
  <span class="n">func_rref</span><span class="p">(</span><span class="n">Example</span><span class="p">());</span> <span class="c1">// rvalue</span>
  <span class="c1">// func_rref(e_ref);     // lvalue reference</span>
  <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="c1">/// !! WARN: every named variable is lvalue, so it can't be passed to rvalue</span>
  <span class="c1">/// reference</span>
  <span class="c1">// func_rref(e_rref); // rvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="n">e_const</span><span class="p">;</span>
  <span class="c1">// func_rref(e_const);                               // const lvalue</span>
  <span class="c1">// func_rref(static_cast&lt;const Example&gt;(Example())); // const rvalue</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_const_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="c1">// func_rref(e_const_ref); // const lvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_const_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="c1">// func_rref(e_const_rref); // const rvalue reference</span>
<span class="p">}</span>

<span class="c1">// test func_const, to pass various types of objects into it</span>
<span class="kt">void</span> <span class="nf">test_func_const</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">Example</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">func_const</span><span class="p">(</span><span class="n">e</span><span class="p">);</span>         <span class="c1">// lvalue</span>
  <span class="n">func_const</span><span class="p">(</span><span class="n">Example</span><span class="p">());</span> <span class="c1">// rvalue</span>
  <span class="n">func_const</span><span class="p">(</span><span class="n">e_ref</span><span class="p">);</span>     <span class="c1">// lvalue reference</span>
  <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="n">func_const</span><span class="p">(</span><span class="n">e_rref</span><span class="p">);</span> <span class="c1">// rvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="n">e_const</span><span class="p">;</span>
  <span class="n">func_const</span><span class="p">(</span><span class="n">e_const</span><span class="p">);</span>                               <span class="c1">// const lvalue</span>
  <span class="n">func_const</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="k">const</span> <span class="n">Example</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Example</span><span class="p">()));</span> <span class="c1">// const rvalue</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_const_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">func_const</span><span class="p">(</span><span class="n">e_const_ref</span><span class="p">);</span> <span class="c1">// const lvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_const_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="n">func_const</span><span class="p">(</span><span class="n">e_const_rref</span><span class="p">);</span> <span class="c1">// const rvalue reference</span>
<span class="p">}</span>

<span class="c1">// test func_const_lref, to pass various types of objects into it</span>
<span class="kt">void</span> <span class="nf">test_func_const_lref</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">Example</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">func_const_lref</span><span class="p">(</span><span class="n">e</span><span class="p">);</span>         <span class="c1">// lvalue</span>
  <span class="n">func_const_lref</span><span class="p">(</span><span class="n">Example</span><span class="p">());</span> <span class="c1">// rvalue</span>
  <span class="n">func_const_lref</span><span class="p">(</span><span class="n">e_ref</span><span class="p">);</span>     <span class="c1">// lvalue reference</span>
  <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="n">func_const_lref</span><span class="p">(</span><span class="n">e_rref</span><span class="p">);</span> <span class="c1">// rvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="n">e_const</span><span class="p">;</span>
  <span class="n">func_const_lref</span><span class="p">(</span><span class="n">e_const</span><span class="p">);</span>                               <span class="c1">// const lvalue</span>
  <span class="n">func_const_lref</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="k">const</span> <span class="n">Example</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Example</span><span class="p">()));</span> <span class="c1">// const rvalue</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_const_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">func_const_lref</span><span class="p">(</span><span class="n">e_const_ref</span><span class="p">);</span> <span class="c1">// const lvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_const_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="n">func_const_lref</span><span class="p">(</span><span class="n">e_const_rref</span><span class="p">);</span> <span class="c1">// const rvalue reference</span>
<span class="p">}</span>

<span class="c1">// test func_const_rref, to pass various types of objects into it</span>
<span class="kt">void</span> <span class="nf">test_func_const_rref</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">Example</span> <span class="n">e</span><span class="p">;</span>
  <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="c1">// func_const_rref(e); // lvalue</span>
  <span class="n">func_const_rref</span><span class="p">(</span><span class="n">Example</span><span class="p">());</span> <span class="c1">// rvalue</span>
  <span class="c1">// func_const_rref(e_ref); // lvalue reference</span>
  <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="c1">// func_const_rref(e_rref); // rvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="n">e_const</span><span class="p">;</span>
  <span class="c1">// func_const_rref(e_const);                               // const lvalue</span>
  <span class="n">func_const_rref</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="k">const</span> <span class="n">Example</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Example</span><span class="p">()));</span> <span class="c1">// const rvalue</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;</span><span class="n">e_const_ref</span> <span class="o">=</span> <span class="n">e</span><span class="p">;</span>
  <span class="c1">// func_const_rref(e_const_ref); // const lvalue reference</span>
  <span class="k">const</span> <span class="n">Example</span> <span class="o">&amp;&amp;</span><span class="n">e_const_rref</span> <span class="o">=</span> <span class="n">Example</span><span class="p">();</span>
  <span class="c1">// func_const_rref(e_const_rref); // const rvalue reference</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">test_func</span><span class="p">();</span>
  <span class="n">test_func_lref</span><span class="p">();</span>
  <span class="n">test_func_rref</span><span class="p">();</span>
  <span class="n">test_func_const</span><span class="p">();</span>
  <span class="n">test_func_const_lref</span><span class="p">();</span>
  <span class="n">test_func_const_rref</span><span class="p">();</span>
  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>

</code></pre></div></div>]]></content><author><name></name></author><category term="C++" /></entry><entry><title type="html">Docker与系统编程</title><link href="http://localhost:4000/2024/03/20/Docker%E4%B8%8E%E7%B3%BB%E7%BB%9F%E7%BC%96%E7%A8%8B.html" rel="alternate" type="text/html" title="Docker与系统编程" /><published>2024-03-20T19:22:46+08:00</published><updated>2024-03-20T19:22:46+08:00</updated><id>http://localhost:4000/2024/03/20/Docker%E4%B8%8E%E7%B3%BB%E7%BB%9F%E7%BC%96%E7%A8%8B</id><content type="html" xml:base="http://localhost:4000/2024/03/20/Docker%E4%B8%8E%E7%B3%BB%E7%BB%9F%E7%BC%96%E7%A8%8B.html"><![CDATA[<ul id="markdown-toc">
  <li><a href="#参考文章" id="markdown-toc-参考文章">参考文章</a></li>
  <li><a href="#操作系统支持" id="markdown-toc-操作系统支持">操作系统支持</a>    <ul>
      <li><a href="#dup2修改文件描述符" id="markdown-toc-dup2修改文件描述符">dup2修改文件描述符</a></li>
      <li><a href="#tty虚拟终端" id="markdown-toc-tty虚拟终端">tty虚拟终端</a></li>
      <li><a href="#pipe管道ipc通信" id="markdown-toc-pipe管道ipc通信">pipe管道IPC通信</a></li>
    </ul>
  </li>
  <li><a href="#docker-cli工作原理" id="markdown-toc-docker-cli工作原理">Docker CLI工作原理</a></li>
</ul>

<h1 id="参考文章">参考文章</h1>

<ol>
  <li>
    <p><a href="https://www.linusakesson.net/programming/tty/">https://www.linusakesson.net/programming/tty/</a></p>

    <p>这篇文章对tty的解释非常到位，可以说是最有价值的关于tty的文章</p>
  </li>
  <li>
    <p><a href="https://www.rkoucha.fr/tech_corner/pty_pdip.html">https://www.rkoucha.fr/tech_corner/pty_pdip.html</a></p>

    <p>这篇主要提供了tty的编程代码</p>
  </li>
  <li>
    <p><a href="https://man7.org/linux/man-pages/man2/dup.2.html">https://man7.org/linux/man-pages/man2/dup.2.html</a></p>

    <p>修改文件描述符</p>
  </li>
  <li>
    <p><a href="https://man7.org/linux/man-pages/man2/pipe.2.html">https://man7.org/linux/man-pages/man2/pipe.2.html</a></p>

    <p>pipe管道IPC通信</p>
  </li>
</ol>

<h1 id="操作系统支持">操作系统支持</h1>

<p>如果想理解Docker CLI与Docker Daemon的交互，必须了解底层操作系统的支持</p>

<h2 id="dup2修改文件描述符">dup2修改文件描述符</h2>

<p>Linux操作系统下可以使用dup复制文件描述符，可以使用dup2来修改文件描述符的指向：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;chrono&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;fstream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;ostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;string&gt;</span><span class="cp">
</span>
<span class="cp">#include</span> <span class="cpf">&lt;fcntl.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;string.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;thread&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;unistd.h&gt;</span><span class="cp">
</span>
<span class="cp">#define MAX_BUFFER_SIZE 1024
</span>
<span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="nf">readLineFromFd</span><span class="p">(</span><span class="kt">int</span> <span class="n">fd</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">char</span> <span class="n">buff</span><span class="p">[</span><span class="n">MAX_BUFFER_SIZE</span> <span class="o">-</span> <span class="mi">1</span><span class="p">];</span>
  <span class="n">memset</span><span class="p">(</span><span class="n">buff</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">MAX_BUFFER_SIZE</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
  <span class="kt">char</span> <span class="n">c</span><span class="p">;</span>
  <span class="kt">int</span> <span class="n">cnt</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
  <span class="k">while</span> <span class="p">(</span><span class="n">read</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">c</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="o">&amp;&amp;</span> <span class="n">cnt</span> <span class="o">!=</span> <span class="n">MAX_BUFFER_SIZE</span> <span class="o">-</span> <span class="mi">2</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">c</span> <span class="o">==</span> <span class="sc">'\n'</span><span class="p">)</span> <span class="p">{</span>
      <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">(</span><span class="n">buff</span><span class="p">);</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
      <span class="n">buff</span><span class="p">[</span><span class="n">cnt</span><span class="p">]</span> <span class="o">=</span> <span class="n">c</span><span class="p">;</span>
      <span class="o">++</span><span class="n">cnt</span><span class="p">;</span>
    <span class="p">}</span>
  <span class="p">}</span>
  <span class="c1">// EOF</span>
  <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">(</span><span class="n">buff</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
  <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>

    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"Enter a command or filename:"</span><span class="p">;</span>
    <span class="n">fflush</span><span class="p">(</span><span class="n">stdout</span><span class="p">);</span>
    <span class="c1">/// std::cout when connect to tty is line buffered; otherwise is full</span>
    <span class="c1">/// buffered std::cin and std::cerr will flush std::cout automatically</span>
    <span class="c1">/// getline will read until '/n', '/n' will be extracted, but not stored</span>
    <span class="k">auto</span> <span class="n">line</span> <span class="o">=</span> <span class="n">readLineFromFd</span><span class="p">(</span><span class="n">STDIN_FILENO</span><span class="p">);</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">line</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">exit</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"You entered: "</span> <span class="o">&lt;&lt;</span> <span class="n">line</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>

    <span class="c1">// If input is *.input, open the file and print every line</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">line</span><span class="p">.</span><span class="n">find</span><span class="p">(</span><span class="s">".input"</span><span class="p">)</span> <span class="o">!=</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">::</span><span class="n">npos</span><span class="p">)</span> <span class="p">{</span>
      <span class="k">auto</span> <span class="n">file</span> <span class="o">=</span> <span class="n">open</span><span class="p">(</span><span class="n">line</span><span class="p">.</span><span class="n">c_str</span><span class="p">(),</span> <span class="n">O_RDONLY</span><span class="p">);</span>
      <span class="k">if</span> <span class="p">(</span><span class="n">file</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cerr</span> <span class="o">&lt;&lt;</span> <span class="s">"Error opening file"</span>
                  <span class="o">&lt;&lt;</span> <span class="s">" Error code:"</span> <span class="o">&lt;&lt;</span> <span class="n">file</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
        <span class="n">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
      <span class="p">}</span>

      <span class="c1">// Redirect stdin to the file</span>
      <span class="kt">int</span> <span class="n">original_stdin</span> <span class="o">=</span> <span class="n">dup</span><span class="p">(</span><span class="n">STDIN_FILENO</span><span class="p">);</span>
      <span class="n">dup2</span><span class="p">(</span><span class="n">file</span><span class="p">,</span> <span class="n">STDIN_FILENO</span><span class="p">);</span>
      <span class="n">close</span><span class="p">(</span><span class="n">file</span><span class="p">);</span>

      <span class="c1">// Print every line</span>
      <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">line</span> <span class="o">=</span> <span class="n">readLineFromFd</span><span class="p">(</span><span class="n">STDIN_FILENO</span><span class="p">);</span>
      <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">this_thread</span><span class="o">::</span><span class="n">sleep_for</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">chrono</span><span class="o">::</span><span class="n">seconds</span><span class="p">(</span><span class="mi">1</span><span class="p">));</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">line</span> <span class="o">==</span> <span class="s">"ChangeStdinToTerminal"</span><span class="p">)</span> <span class="p">{</span>
          <span class="c1">// Switch stdin back to terminal</span>
          <span class="k">if</span> <span class="p">(</span><span class="n">dup2</span><span class="p">(</span><span class="n">original_stdin</span><span class="p">,</span> <span class="n">STDIN_FILENO</span><span class="p">)</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">exit</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span>
          <span class="p">};</span>
          <span class="n">close</span><span class="p">(</span><span class="n">original_stdin</span><span class="p">);</span>
          <span class="k">break</span><span class="p">;</span>
        <span class="p">}</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">line</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
        <span class="n">line</span> <span class="o">=</span> <span class="n">readLineFromFd</span><span class="p">(</span><span class="n">STDIN_FILENO</span><span class="p">);</span>
      <span class="p">}</span>
    <span class="p">}</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="tty虚拟终端">tty虚拟终端</h2>

<p>操作系统内核提供的虚拟终端支持，以下代码来zhttp://www.rkoucha.fr/tech_corner/pty_pdip.html：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">/// demo from :http://www.rkoucha.fr/tech_corner/pty_pdip.html</span>
<span class="cp">#define _XOPEN_SOURCE 600
#include</span> <span class="cpf">&lt;errno.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;fcntl.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;unistd.h&gt;</span><span class="cp">
#define __USE_BSD
#include</span> <span class="cpf">&lt;termios.h&gt;</span><span class="cp">
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="n">fdm</span><span class="p">,</span> <span class="n">fds</span><span class="p">,</span> <span class="n">rc</span><span class="p">;</span>
  <span class="kt">char</span> <span class="n">input</span><span class="p">[</span><span class="mi">150</span><span class="p">];</span>

  <span class="n">fdm</span> <span class="o">=</span> <span class="n">posix_openpt</span><span class="p">(</span><span class="n">O_RDWR</span><span class="p">);</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">fdm</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Error %d on posix_openpt()</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">errno</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">1</span><span class="p">;</span>
  <span class="p">}</span>

  <span class="n">rc</span> <span class="o">=</span> <span class="n">grantpt</span><span class="p">(</span><span class="n">fdm</span><span class="p">);</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">rc</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Error %d on grantpt()</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">errno</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">1</span><span class="p">;</span>
  <span class="p">}</span>

  <span class="n">rc</span> <span class="o">=</span> <span class="n">unlockpt</span><span class="p">(</span><span class="n">fdm</span><span class="p">);</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">rc</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"Error %d on unlockpt()</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">errno</span><span class="p">);</span>
    <span class="k">return</span> <span class="mi">1</span><span class="p">;</span>
  <span class="p">}</span>

  <span class="c1">// Open the slave PTY</span>
  <span class="n">fds</span> <span class="o">=</span> <span class="n">open</span><span class="p">(</span><span class="n">ptsname</span><span class="p">(</span><span class="n">fdm</span><span class="p">),</span> <span class="n">O_RDWR</span><span class="p">);</span>

  <span class="c1">// Creation of a child process</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">fork</span><span class="p">())</span> <span class="p">{</span>
    <span class="c1">// Father</span>

    <span class="c1">// Close the slave side of the PTY</span>
    <span class="n">close</span><span class="p">(</span><span class="n">fds</span><span class="p">);</span>
    <span class="k">while</span> <span class="p">(</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
      <span class="c1">// Operator's entry (standard input = terminal)</span>
      <span class="n">write</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="s">"Input : "</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="s">"Input : "</span><span class="p">));</span>
      <span class="n">rc</span> <span class="o">=</span> <span class="n">read</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">input</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">input</span><span class="p">));</span>
      <span class="k">if</span> <span class="p">(</span><span class="n">rc</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Send the input to the child process through the PTY</span>
        <span class="n">write</span><span class="p">(</span><span class="n">fdm</span><span class="p">,</span> <span class="n">input</span><span class="p">,</span> <span class="n">rc</span><span class="p">);</span>

        <span class="c1">// Get the child's answer through the PTY</span>
        <span class="n">rc</span> <span class="o">=</span> <span class="n">read</span><span class="p">(</span><span class="n">fdm</span><span class="p">,</span> <span class="n">input</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">input</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">rc</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
          <span class="c1">// Make the answer NUL terminated to display it as a string</span>
          <span class="n">input</span><span class="p">[</span><span class="n">rc</span><span class="p">]</span> <span class="o">=</span> <span class="sc">'\0'</span><span class="p">;</span>

          <span class="n">fprintf</span><span class="p">(</span><span class="n">stderr</span><span class="p">,</span> <span class="s">"%s"</span><span class="p">,</span> <span class="n">input</span><span class="p">);</span>
        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
          <span class="k">break</span><span class="p">;</span>
        <span class="p">}</span>
      <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="k">break</span><span class="p">;</span>
      <span class="p">}</span>
    <span class="p">}</span> <span class="c1">// End while</span>
  <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
    <span class="k">struct</span> <span class="nc">termios</span> <span class="n">slave_orig_term_settings</span><span class="p">;</span> <span class="c1">// Saved terminal settings</span>
    <span class="k">struct</span> <span class="nc">termios</span> <span class="n">new_term_settings</span><span class="p">;</span>        <span class="c1">// Current terminal settings</span>

    <span class="c1">// Child</span>

    <span class="c1">// Close the master side of the PTY</span>
    <span class="n">close</span><span class="p">(</span><span class="n">fdm</span><span class="p">);</span>

    <span class="c1">// Save the default parameters of the slave side of the PTY</span>
    <span class="n">rc</span> <span class="o">=</span> <span class="n">tcgetattr</span><span class="p">(</span><span class="n">fds</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">slave_orig_term_settings</span><span class="p">);</span>

    <span class="c1">// Set raw mode on the slave side of the PTY</span>
    <span class="n">new_term_settings</span> <span class="o">=</span> <span class="n">slave_orig_term_settings</span><span class="p">;</span>
    <span class="n">cfmakeraw</span><span class="p">(</span><span class="o">&amp;</span><span class="n">new_term_settings</span><span class="p">);</span>
    <span class="n">tcsetattr</span><span class="p">(</span><span class="n">fds</span><span class="p">,</span> <span class="n">TCSANOW</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">new_term_settings</span><span class="p">);</span>

    <span class="c1">// The slave side of the PTY becomes the standard input and outputs of the</span>
    <span class="c1">// child process</span>
    <span class="n">close</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span> <span class="c1">// Close standard input (current terminal)</span>
    <span class="n">close</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span> <span class="c1">// Close standard output (current terminal)</span>
    <span class="n">close</span><span class="p">(</span><span class="mi">2</span><span class="p">);</span> <span class="c1">// Close standard error (current terminal)</span>

    <span class="n">dup</span><span class="p">(</span><span class="n">fds</span><span class="p">);</span> <span class="c1">// PTY becomes standard input (0)</span>
    <span class="n">dup</span><span class="p">(</span><span class="n">fds</span><span class="p">);</span> <span class="c1">// PTY becomes standard output (1)</span>
    <span class="n">dup</span><span class="p">(</span><span class="n">fds</span><span class="p">);</span> <span class="c1">// PTY becomes standard error (2)</span>

    <span class="k">while</span> <span class="p">(</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
      <span class="n">rc</span> <span class="o">=</span> <span class="n">read</span><span class="p">(</span><span class="n">fds</span><span class="p">,</span> <span class="n">input</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">input</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>

      <span class="k">if</span> <span class="p">(</span><span class="n">rc</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// Replace the terminating \n by a NUL to display it as a string</span>
        <span class="n">input</span><span class="p">[</span><span class="n">rc</span> <span class="o">-</span> <span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="sc">'\0'</span><span class="p">;</span>

        <span class="n">printf</span><span class="p">(</span><span class="s">"Child received : '%s'</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">input</span><span class="p">);</span>
      <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="k">break</span><span class="p">;</span>
      <span class="p">}</span>
    <span class="p">}</span> <span class="c1">// End while</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span> <span class="c1">// main</span>
</code></pre></div></div>

<h2 id="pipe管道ipc通信">pipe管道IPC通信</h2>

<p>Linux操作系统支持使用管道的IPC通信：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;stdlib.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;sys/types.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;unistd.h&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;sys/wait.h&gt;</span><span class="cp">
</span>
<span class="cp">#define BUFFER_SIZE 25
</span>
<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
  <span class="kt">int</span> <span class="n">pipefd</span><span class="p">[</span><span class="mi">2</span><span class="p">];</span>
  <span class="n">pid_t</span> <span class="n">pid</span><span class="p">;</span>
  <span class="kt">char</span> <span class="n">buffer</span><span class="p">[</span><span class="n">BUFFER_SIZE</span><span class="p">];</span>

  <span class="c1">// Create pipe</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">pipe</span><span class="p">(</span><span class="n">pipefd</span><span class="p">)</span> <span class="o">==</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">perror</span><span class="p">(</span><span class="s">"pipe"</span><span class="p">);</span>
    <span class="n">exit</span><span class="p">(</span><span class="n">EXIT_FAILURE</span><span class="p">);</span>
  <span class="p">}</span>

  <span class="c1">// Fork a child process</span>
  <span class="n">pid</span> <span class="o">=</span> <span class="n">fork</span><span class="p">();</span>

  <span class="k">if</span> <span class="p">(</span><span class="n">pid</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">perror</span><span class="p">(</span><span class="s">"fork"</span><span class="p">);</span>
    <span class="n">exit</span><span class="p">(</span><span class="n">EXIT_FAILURE</span><span class="p">);</span>
  <span class="p">}</span>

  <span class="k">if</span> <span class="p">(</span><span class="n">pid</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>     <span class="c1">// Child process</span>
    <span class="n">close</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">1</span><span class="p">]);</span> <span class="c1">// Close the write end of the pipe</span>

    <span class="c1">// Read from the pipe</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Child process reading from the pipe...</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">read</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">buffer</span><span class="p">,</span> <span class="n">BUFFER_SIZE</span><span class="p">);</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Child process received: %s</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">buffer</span><span class="p">);</span>

    <span class="n">close</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span> <span class="c1">// Close the read end of the pipe</span>
    <span class="n">exit</span><span class="p">(</span><span class="n">EXIT_SUCCESS</span><span class="p">);</span>
  <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>            <span class="c1">// Parent process</span>
    <span class="n">close</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">0</span><span class="p">]);</span> <span class="c1">// Close the read end of the pipe</span>

    <span class="c1">// Write to the pipe</span>
    <span class="n">printf</span><span class="p">(</span><span class="s">"Parent process writing to the pipe...</span><span class="se">\n</span><span class="s">"</span><span class="p">);</span>
    <span class="n">write</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="s">"Hello from parent!"</span><span class="p">,</span> <span class="mi">18</span><span class="p">);</span>

    <span class="n">close</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">1</span><span class="p">]);</span> <span class="c1">// Close the write end of the pipe</span>
    <span class="n">wait</span><span class="p">(</span><span class="nb">NULL</span><span class="p">);</span>       <span class="c1">// Wait for the child process to finish</span>
    <span class="n">exit</span><span class="p">(</span><span class="n">EXIT_SUCCESS</span><span class="p">);</span>
  <span class="p">}</span>

  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h1 id="docker-cli工作原理">Docker CLI工作原理</h1>

<p>在了解了底层操作系统的支持后，下面是我整理画出的Docker CLI与容器的交互过程（不保证正确）：</p>

<p><img src="/assets/images/docker_cli.drawio.png" alt="docker_cli.drawio.png" /></p>

<p>说明如下：</p>

<ol>
  <li>当运行Docker CLI程序，例如<code class="language-plaintext highlighter-rouge">docker run</code>， <code class="language-plaintext highlighter-rouge">docker attach</code>等，这些程序会跟Docker Daemon通信；Docker CLI与Docker Daemon是客户/服务的工作模式</li>
  <li>Docker CLI与Docker Daemon的通信可以使用本机的unix domain socket，也可以是远程的网络连接，比如TCP等</li>
  <li>真正的容器操作，例如创建运行容器、attach容器、关闭容器等操作都是由Docker Daemon完成的，所以Docker Daemon是整个Docker系统的核心组件</li>
  <li>当使用<code class="language-plaintext highlighter-rouge">docker run</code> <code class="language-plaintext highlighter-rouge">-d</code>选项运行一个镜像时，Docker CLI与Docker Daemon之间的连接被关闭，Docker CLI程序执行完毕，但是运行的容器跟这个选项没有任何关系，用户随时可以使用<code class="language-plaintext highlighter-rouge">docker attach</code>命令来重新与Docker Daemon建立连接</li>
  <li>Docker Daemon与创建的容器进程（运行在与Host独立的namespace中）之间的通信可以是<code class="language-plaintext highlighter-rouge">pipe</code>，也可以是<code class="language-plaintext highlighter-rouge">tty</code>。Docker Daemon通过这些通信手段来控制容器进程的标准输入</li>
  <li>当使用<code class="language-plaintext highlighter-rouge">docker run</code> <code class="language-plaintext highlighter-rouge">-t</code>，选项时，会为容器进程分配一个<code class="language-plaintext highlighter-rouge">tty</code>，注意这个<code class="language-plaintext highlighter-rouge">tty</code>是Host内核中的<code class="language-plaintext highlighter-rouge">tty</code>，但是工作在容器进程这个独立与Host 的namespace中</li>
  <li>如果不使用<code class="language-plaintext highlighter-rouge">-t</code>，则会直接使用<code class="language-plaintext highlighter-rouge">pipe</code>与容器进程通信</li>
  <li>如果使用<code class="language-plaintext highlighter-rouge">docker run</code> <code class="language-plaintext highlighter-rouge">-i</code>，则是告诉Docker Daemon保持容器进程的标准输入为打开状态，即不关闭<code class="language-plaintext highlighter-rouge">tty</code>或者<code class="language-plaintext highlighter-rouge">pipe</code>的写入端</li>
  <li>如果不使用<code class="language-plaintext highlighter-rouge">-i</code>，则Docker Daemon会关闭<code class="language-plaintext highlighter-rouge">tty</code>或者<code class="language-plaintext highlighter-rouge">pipe</code>的写入端，如此容器进程的程序会在标准输入读到<code class="language-plaintext highlighter-rouge">EOF</code></li>
</ol>]]></content><author><name></name></author><category term="system_programming" /></entry><entry><title type="html">C++可调用对象</title><link href="http://localhost:4000/2024/01/21/C++-%E5%8F%AF%E8%B0%83%E7%94%A8%E5%AF%B9%E8%B1%A1.html" rel="alternate" type="text/html" title="C++可调用对象" /><published>2024-01-21T19:22:46+08:00</published><updated>2024-01-21T19:22:46+08:00</updated><id>http://localhost:4000/2024/01/21/C++-%E5%8F%AF%E8%B0%83%E7%94%A8%E5%AF%B9%E8%B1%A1</id><content type="html" xml:base="http://localhost:4000/2024/01/21/C++-%E5%8F%AF%E8%B0%83%E7%94%A8%E5%AF%B9%E8%B1%A1.html"><![CDATA[<p>本文试图在作者的知识范围内解释清楚C++中<code class="language-plaintext highlighter-rouge">Callable</code>这个概念。通常<code class="language-plaintext highlighter-rouge">Callable</code>总是跟<code class="language-plaintext highlighter-rouge">函数</code>直接等价，但是同时也包括其他可调用对象，例如<code class="language-plaintext highlighter-rouge">std::function</code>、<code class="language-plaintext highlighter-rouge">std::bind</code>、lamda表达式、成员函数、可调用对象(实现<code class="language-plaintext highlighter-rouge">()</code>操作符的类)等。以下内容<code class="language-plaintext highlighter-rouge">函数</code>特指全局定义的函数。</p>

<ul id="markdown-toc">
  <li><a href="#函数是一种类型" id="markdown-toc-函数是一种类型">函数是一种类型</a></li>
  <li><a href="#函数类型不是一等公民" id="markdown-toc-函数类型不是一等公民">函数类型不是一等公民</a>    <ul>
      <li><a href="#什么是编程语言中的一等公民" id="markdown-toc-什么是编程语言中的一等公民">什么是编程语言中的一等公民</a></li>
      <li><a href="#函数不可被赋值" id="markdown-toc-函数不可被赋值">函数不可被赋值</a></li>
      <li><a href="#函数可以做等号运算和函数参数" id="markdown-toc-函数可以做等号运算和函数参数">函数可以做等号运算和函数参数</a></li>
      <li><a href="#函数不可以返回函数类型" id="markdown-toc-函数不可以返回函数类型">函数不可以返回函数类型</a></li>
      <li><a href="#函数指针是一等公民" id="markdown-toc-函数指针是一等公民">函数指针是一等公民</a></li>
      <li><a href="#成员函数是一种特殊的函数" id="markdown-toc-成员函数是一种特殊的函数">成员函数是一种特殊的函数</a></li>
    </ul>
  </li>
  <li><a href="#函数对象function-object" id="markdown-toc-函数对象function-object">函数对象(function object)</a>    <ul>
      <li><a href="#stdbind" id="markdown-toc-stdbind">std::bind</a></li>
      <li><a href="#stdfunction" id="markdown-toc-stdfunction">std::function</a>        <ul>
          <li><a href="#stdfunction内存分配" id="markdown-toc-stdfunction内存分配">std::function内存分配</a></li>
        </ul>
      </li>
      <li><a href="#lamda-expression" id="markdown-toc-lamda-expression">lamda expression</a></li>
    </ul>
  </li>
  <li><a href="#可调用对象callable" id="markdown-toc-可调用对象callable">可调用对象（Callable）</a>    <ul>
      <li><a href="#callable就是能被stdinvoke调用的对象" id="markdown-toc-callable就是能被stdinvoke调用的对象">Callable就是能被std::invoke调用的对象</a></li>
    </ul>
  </li>
</ul>

<h1 id="函数是一种类型">函数是一种类型</h1>

<p>在任何讨论之前，有一点需要明确的是C++中<code class="language-plaintext highlighter-rouge">函数</code>是一种类型<code class="language-plaintext highlighter-rouge">type</code>, 这种类型唯一的定义了一个<code class="language-plaintext highlighter-rouge">signature</code>；类型可以被声明，在该类型下可以有多个函数定义：</p>

<p>例1：</p>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 类型名称：void(int*);函数名称：func1;signature:void(int*)</span>
<span class="kt">void</span> <span class="nf">func1</span><span class="p">(</span><span class="kt">int</span> <span class="o">*</span><span class="p">);</span>

<span class="c1">// 类型名称：type_func; signature: void(int*)</span>
<span class="k">typedef</span> <span class="kt">void</span> <span class="nf">type_func</span><span class="p">(</span><span class="kt">int</span> <span class="o">*</span><span class="p">);</span>

<span class="c1">// 类型名称为void(int*)的函数</span>
<span class="n">type_func</span> <span class="n">func2</span><span class="p">;</span>

<span class="c1">// 函数定义</span>
<span class="kt">void</span> <span class="nf">func1</span><span class="p">(</span><span class="kt">int</span> <span class="o">*</span><span class="p">)</span> <span class="p">{}</span>
<span class="kt">void</span> <span class="nf">func2</span><span class="p">(</span><span class="kt">int</span> <span class="o">*</span><span class="p">)</span> <span class="p">{}</span>
</code></pre></div></div>

<h1 id="函数类型不是一等公民">函数类型不是一等公民</h1>

<h2 id="什么是编程语言中的一等公民">什么是编程语言中的一等公民</h2>

<p>首先可以阅读<a href="https://en.wikipedia.org/wiki/First-class_citizen#:~:text=In%20a%20given%20programming%20language,and%20assigned%20to%20a%20variable.">Wikipedia中的解释</a>。总结下来，就是要全部满足如下几个条件：</p>

<ul>
  <li>这个实体可以被赋值</li>
  <li>这个实体可以做<code class="language-plaintext highlighter-rouge">==</code>运算</li>
  <li>这个实体可以作为函数的参数传入</li>
  <li>这个实体可以作为函数的返回值</li>
</ul>

<h2 id="函数不可被赋值">函数不可被赋值</h2>

<p>在例1中，不可以做如下操作：</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">type_func</span> <span class="n">func3</span> <span class="o">=</span> <span class="n">func1</span><span class="p">;</span>
</code></pre></div></div>
<p>因为在C++中，只有<code class="language-plaintext highlighter-rouge">变量</code>可以被赋值，<code class="language-plaintext highlighter-rouge">func3</code>, <code class="language-plaintext highlighter-rouge">func4</code>都不是变量，是<code class="language-plaintext highlighter-rouge">函数</code>。</p>

<h2 id="函数可以做等号运算和函数参数">函数可以做等号运算和函数参数</h2>

<p>在例1的基础上，如果编译运行如下代码：</p>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">func1</span> <span class="o">==</span> <span class="n">func2</span><span class="p">){</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func1 is equal to func2"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="p">}</span><span class="k">else</span><span class="p">{</span>
        <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func1 is not equal to func2"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>
<p>程序能够正常编译运行，输出”func1 is not equal to func2”。但这不是因为函数类型本身做了等号运算，而是函数名称被隐式转换为函数指针类型:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="nf">compare_func</span><span class="p">(</span><span class="n">type_func</span> <span class="n">func1</span><span class="p">,</span> <span class="n">type_func</span> <span class="n">func2</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">func1</span> <span class="o">==</span> <span class="n">func2</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func1 is equal to func2"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
  <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">boolalpha</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">is_pointer</span><span class="o">&lt;</span><span class="k">decltype</span><span class="p">(</span><span class="n">func1</span><span class="p">)</span><span class="o">&gt;::</span><span class="n">value</span>
              <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">boolalpha</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">is_pointer</span><span class="o">&lt;</span><span class="k">decltype</span><span class="p">(</span><span class="n">func2</span><span class="p">)</span><span class="o">&gt;::</span><span class="n">value</span>
              <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func1 is not equal to func2"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span> <span class="n">compare_func</span><span class="p">(</span><span class="n">func1</span><span class="p">,</span> <span class="n">func2</span><span class="p">);</span> <span class="p">}</span>
</code></pre></div></div>

<ul>
  <li>函数可以做等号运算，也可以作为函数参数传入；但为隐式转换为函数指针类型</li>
</ul>

<h2 id="函数不可以返回函数类型">函数不可以返回函数类型</h2>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 错误，不可以直接返回函数类型</span>
<span class="n">type_func</span> <span class="nf">ret_func1</span><span class="p">()</span> <span class="p">{</span> <span class="k">return</span> <span class="n">func1</span><span class="p">;</span> <span class="p">}</span>
<span class="c1">// 正确，可以返回函数指针类型</span>
<span class="n">type_func</span> <span class="o">*</span><span class="nf">ret_func2</span><span class="p">()</span> <span class="p">{</span> <span class="k">return</span> <span class="n">func2</span><span class="p">;</span> <span class="p">}</span>
</code></pre></div></div>

<h2 id="函数指针是一等公民">函数指针是一等公民</h2>

<p>综上，C++函数类型本身不是一等公民，但是其指针是一等公民；而在函数作为函数参数和等号比较运算时会隐式转换为函数指针，让函数类型具备了一等公民的某些特点。</p>

<h2 id="成员函数是一种特殊的函数">成员函数是一种特殊的函数</h2>

<p>成员函数是一种特殊的函数：</p>
<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;functional&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;type_traits&gt;</span><span class="cp">
</span>
<span class="k">class</span> <span class="nc">SampleClass</span> <span class="p">{</span>
<span class="nl">public:</span>
  <span class="n">SampleClass</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="o">&amp;</span><span class="n">spc</span><span class="p">)</span> <span class="o">:</span> <span class="n">spec</span><span class="p">{</span><span class="n">spc</span><span class="p">}</span> <span class="p">{}</span>
  <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">spec</span><span class="p">;</span>

  <span class="kt">void</span> <span class="nf">print</span><span class="p">();</span>
<span class="p">};</span>

<span class="kt">void</span> <span class="n">SampleClass</span><span class="o">::</span><span class="n">print</span><span class="p">()</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="k">this</span><span class="o">-&gt;</span><span class="n">spec</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>

  <span class="n">SampleClass</span> <span class="n">myclass</span><span class="p">{</span><span class="s">"hello"</span><span class="p">};</span>

  <span class="c1">// 指向成员函数的类型,注意括号中包含类的名称</span>
  <span class="k">typedef</span> <span class="nf">void</span> <span class="p">(</span><span class="n">SampleClass</span><span class="o">::*</span><span class="n">mem_ptr</span><span class="p">)();</span>

  <span class="n">mem_ptr</span> <span class="n">ptr</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">SampleClass</span><span class="o">::</span><span class="n">print</span><span class="p">;</span>
  <span class="c1">// 调用print</span>
  <span class="p">(</span><span class="n">myclass</span><span class="p">.</span><span class="o">*</span><span class="n">ptr</span><span class="p">)();</span>

  <span class="c1">// C++17以上的编译器，可以使用如下</span>
  <span class="n">std</span><span class="o">::</span><span class="n">invoke</span><span class="p">(</span><span class="n">ptr</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">myclass</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<h1 id="函数对象function-object">函数对象(function object)</h1>

<p>如果一个类型实现了<code class="language-plaintext highlighter-rouge">()</code>操作符，则该类型是一个函数对象。函数对象可以像函数一样被调用:</p>

<div class="language-c++ highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;functional&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="k">class</span> <span class="nc">FunctorClass</span> <span class="p">{</span>
<span class="nl">public:</span>
  <span class="kt">int</span> <span class="n">a</span><span class="p">{</span><span class="mi">1</span><span class="p">};</span>

  <span class="kt">void</span> <span class="nf">operator</span><span class="p">()()</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">a</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>
<span class="p">};</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">FunctorClass</span> <span class="n">f</span><span class="p">;</span>
  <span class="n">f</span><span class="p">();</span>

  <span class="c1">// since C++17</span>
  <span class="n">std</span><span class="o">::</span><span class="n">invoke</span><span class="p">(</span><span class="n">f</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="stdbind">std::bind</h2>

<p><a href="https://en.cppreference.com/w/cpp/utility/functional/bind">std::bind</a>接收一个<code class="language-plaintext highlighter-rouge">Callable</code>，返回一个函数对象。<code class="language-plaintext highlighter-rouge">Callable</code>可以是：</p>
<ul>
  <li>函数指针</li>
  <li>类成员函数指针</li>
  <li>类成员指针（成员指针，虽然没有调用，但是一个<code class="language-plaintext highlighter-rouge">Callable</code>）</li>
  <li>函数对象</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">std::bind</code>是一个包装器，跟<code class="language-plaintext highlighter-rouge">std::function</code>类似。作用是将预定义的参数绑定到对应的<code class="language-plaintext highlighter-rouge">Callable</code>上:</p>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;functional&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;random&gt;</span><span class="cp">
</span>
<span class="kt">void</span> <span class="nf">f</span><span class="p">(</span><span class="kt">int</span> <span class="n">n1</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n2</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n3</span><span class="p">,</span> <span class="k">const</span> <span class="kt">int</span> <span class="o">&amp;</span><span class="n">n4</span><span class="p">,</span> <span class="kt">int</span> <span class="n">n5</span><span class="p">)</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">n1</span> <span class="o">&lt;&lt;</span> <span class="sc">' '</span> <span class="o">&lt;&lt;</span> <span class="n">n2</span> <span class="o">&lt;&lt;</span> <span class="sc">' '</span> <span class="o">&lt;&lt;</span> <span class="n">n3</span> <span class="o">&lt;&lt;</span> <span class="sc">' '</span> <span class="o">&lt;&lt;</span> <span class="n">n4</span> <span class="o">&lt;&lt;</span> <span class="sc">' '</span> <span class="o">&lt;&lt;</span> <span class="n">n5</span> <span class="o">&lt;&lt;</span> <span class="sc">'\n'</span><span class="p">;</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>

  <span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">7</span><span class="p">;</span>
  <span class="c1">// 占位符表示的是f1被实际调用时，用户传入的参数位置</span>
  <span class="k">auto</span> <span class="n">f1</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">bind</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">placeholders</span><span class="o">::</span><span class="n">_2</span><span class="p">,</span> <span class="mi">42</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">placeholders</span><span class="o">::</span><span class="n">_1</span><span class="p">,</span>
                      <span class="n">std</span><span class="o">::</span><span class="n">cref</span><span class="p">(</span><span class="n">n</span><span class="p">),</span> <span class="n">n</span><span class="p">);</span>

  <span class="n">f1</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">);</span> <span class="c1">// 2,42,1,7,7</span>

  <span class="k">auto</span> <span class="n">f2</span> <span class="o">=</span> <span class="p">[](</span><span class="kt">int</span> <span class="n">a</span><span class="p">,</span> <span class="kt">int</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">a</span> <span class="o">&lt;&lt;</span> <span class="n">b</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">};</span>
  <span class="c1">// bind一个lamda；lamda表达式也是一个函数对象</span>
  <span class="k">auto</span> <span class="n">f3</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">bind</span><span class="p">(</span><span class="n">f2</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">placeholders</span><span class="o">::</span><span class="n">_1</span><span class="p">);</span>

  <span class="n">f3</span><span class="p">(</span><span class="mi">2</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>
<p>需要注意：</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">std::bind</code>返回一个匿名类型的函数对象（function object），类型由编译器自动创建</li>
  <li><code class="language-plaintext highlighter-rouge">std::bind</code>的所有参数都是Universal Reference，所有的传入参数会根据用户传入的参数进行构造
    <ul>
      <li><code class="language-plaintext highlighter-rouge">std::bind</code>返回的对象包含了传入的函数对象和所有参数的实例，这些实例从用户传入的Universal Reference构造
        <ul>
          <li>函数对象和参数的类型必须是<code class="language-plaintext highlighter-rouge">MoveConstructible</code>和<code class="language-plaintext highlighter-rouge">Destructible</code>,否则是UB</li>
        </ul>
      </li>
      <li>如果函数对象以及所有的参数是<code class="language-plaintext highlighter-rouge">CopyConstructible</code>，则std::bind返回的对象是C<code class="language-plaintext highlighter-rouge">opyConstructible</code>，否则是<code class="language-plaintext highlighter-rouge">MoveConstructible</code></li>
    </ul>
  </li>
</ul>

<h2 id="stdfunction">std::function</h2>

<p><a href="https://en.cppreference.com/w/cpp/utility/functional/function">std::function</a>与<code class="language-plaintext highlighter-rouge">std::bind</code>一样，也是一个<code class="language-plaintext highlighter-rouge">Callable</code>的包装器。<code class="language-plaintext highlighter-rouge">std::function</code>对象是可拷贝，可赋值的:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;functional&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="kt">void</span> <span class="nf">f</span><span class="p">(</span><span class="kt">int</span> <span class="o">*</span><span class="p">)</span> <span class="p">{</span> <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"func called"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span> <span class="p">}</span>

<span class="k">class</span> <span class="nc">Funtor</span> <span class="p">{</span>
<span class="nl">public:</span>
  <span class="n">Funtor</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="o">&amp;</span><span class="n">name</span><span class="p">)</span> <span class="o">:</span> <span class="n">name</span><span class="p">{</span><span class="n">name</span><span class="p">}</span> <span class="p">{}</span>
  <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">name</span><span class="p">;</span>
  <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="nf">operator</span><span class="p">()()</span> <span class="p">{</span> <span class="k">return</span> <span class="k">this</span><span class="o">-&gt;</span><span class="n">name</span><span class="p">;</span> <span class="p">}</span>
  <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="nf">get_name</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"get_name called"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="k">return</span> <span class="k">this</span><span class="o">-&gt;</span><span class="n">name</span><span class="p">;</span>
  <span class="p">}</span>
  <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="nf">get_name_2</span><span class="p">()</span> <span class="p">{</span>
    <span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"get_name_2 called"</span> <span class="o">&lt;&lt;</span> <span class="n">std</span><span class="o">::</span><span class="n">endl</span><span class="p">;</span>
    <span class="k">return</span> <span class="k">this</span><span class="o">-&gt;</span><span class="n">name</span><span class="p">;</span>
  <span class="p">}</span>
<span class="p">};</span>

<span class="k">typedef</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="p">(</span><span class="n">Funtor</span><span class="o">::*</span><span class="n">get_name</span><span class="p">)();</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">std</span><span class="o">::</span><span class="n">function</span><span class="o">&lt;</span><span class="kt">void</span><span class="p">(</span><span class="kt">int</span> <span class="o">*</span><span class="p">)</span><span class="o">&gt;</span> <span class="n">funtor</span> <span class="o">=</span> <span class="n">f</span><span class="p">;</span>
  <span class="kt">int</span> <span class="n">a</span><span class="p">;</span>
  <span class="n">funtor</span><span class="p">(</span><span class="o">&amp;</span><span class="n">a</span><span class="p">);</span>
  <span class="c1">// 指向类成员函数的指针</span>
  <span class="n">get_name</span> <span class="n">get</span> <span class="o">=</span> <span class="o">&amp;</span><span class="n">Funtor</span><span class="o">::</span><span class="n">get_name</span><span class="p">;</span>
  <span class="n">Funtor</span> <span class="n">func</span><span class="p">{</span><span class="s">"elela"</span><span class="p">};</span>
  <span class="p">(</span><span class="n">func</span><span class="p">.</span><span class="o">*</span><span class="n">get</span><span class="p">)();</span>

  <span class="c1">// std::bind可以bind指向成员函数的Callable</span>
  <span class="n">std</span><span class="o">::</span><span class="n">bind</span><span class="p">(</span><span class="n">get</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">func</span><span class="p">)();</span>

  <span class="c1">// std::function 不能直接存储一个成员函数指针，需要通过std::bind传递</span>
  <span class="c1">// std::function&lt;get_name&gt; func_wrapper;</span>
  <span class="n">std</span><span class="o">::</span><span class="n">function</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span><span class="o">&gt;</span> <span class="n">func_wrap</span> <span class="o">=</span>
      <span class="n">std</span><span class="o">::</span><span class="n">bind</span><span class="p">(</span><span class="o">&amp;</span><span class="n">Funtor</span><span class="o">::</span><span class="n">get_name_2</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">func</span><span class="p">);</span>
  <span class="n">func_wrap</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>

<h3 id="stdfunction内存分配">std::function内存分配</h3>

<p>在<code class="language-plaintext highlighter-rouge">std::function</code>存储的<code class="language-plaintext highlighter-rouge">Callable</code>较小时（实验测试16字节），std::function存储在栈上，否则会在heap上申请内存：</p>

<p>源码：</p>
<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 是否在栈上的判断条件</span>
	<span class="k">static</span> <span class="k">const</span> <span class="kt">bool</span> <span class="n">__stored_locally</span> <span class="o">=</span>
	<span class="p">(</span><span class="n">__is_location_invariant</span><span class="o">&lt;</span><span class="n">_Functor</span><span class="o">&gt;::</span><span class="n">value</span>
	 <span class="o">&amp;&amp;</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">_Functor</span><span class="p">)</span> <span class="o">&lt;=</span> <span class="n">_M_max_size</span>
	 <span class="o">&amp;&amp;</span> <span class="n">__alignof__</span><span class="p">(</span><span class="n">_Functor</span><span class="p">)</span> <span class="o">&lt;=</span> <span class="n">_M_max_align</span>
	 <span class="o">&amp;&amp;</span> <span class="p">(</span><span class="n">_M_max_align</span> <span class="o">%</span> <span class="n">__alignof__</span><span class="p">(</span><span class="n">_Functor</span><span class="p">)</span> <span class="o">==</span> <span class="mi">0</span><span class="p">));</span>
	<span class="k">typedef</span> <span class="n">integral_constant</span><span class="o">&lt;</span><span class="kt">bool</span><span class="p">,</span> <span class="n">__stored_locally</span><span class="o">&gt;</span> <span class="n">_Local_storage</span><span class="p">;</span>
     <span class="p">...</span>
<span class="c1">//  _M_max_size=sizeof(_Nocopy_types)</span>
  <span class="k">union</span> <span class="n">_Nocopy_types</span>
  <span class="p">{</span>
    <span class="kt">void</span><span class="o">*</span>       <span class="n">_M_object</span><span class="p">;</span>
    <span class="k">const</span> <span class="kt">void</span><span class="o">*</span> <span class="n">_M_const_object</span><span class="p">;</span>
    <span class="kt">void</span> <span class="p">(</span><span class="o">*</span><span class="n">_M_function_pointer</span><span class="p">)();</span>
    <span class="kt">void</span> <span class="p">(</span><span class="n">_Undefined_class</span><span class="o">::*</span><span class="n">_M_member_pointer</span><span class="p">)();</span>
  <span class="p">};</span>
<span class="p">...</span>
<span class="c1">// 内存分配函数◊</span>
	<span class="k">static</span> <span class="kt">void</span>
	<span class="nf">_M_init_functor</span><span class="p">(</span><span class="n">_Any_data</span><span class="o">&amp;</span> <span class="n">__functor</span><span class="p">,</span> <span class="n">_Functor</span><span class="o">&amp;&amp;</span> <span class="n">__f</span><span class="p">)</span>
	<span class="p">{</span> <span class="n">_M_init_functor</span><span class="p">(</span><span class="n">__functor</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">__f</span><span class="p">),</span> <span class="n">_Local_storage</span><span class="p">());</span> <span class="p">}</span>

<span class="p">...</span>
<span class="c1">// 分配判断：如果在栈上则使用placement new，否则使用new</span>
	<span class="k">static</span> <span class="kt">void</span>
	<span class="nf">_M_init_functor</span><span class="p">(</span><span class="n">_Any_data</span><span class="o">&amp;</span> <span class="n">__functor</span><span class="p">,</span> <span class="n">_Functor</span><span class="o">&amp;&amp;</span> <span class="n">__f</span><span class="p">,</span> <span class="n">true_type</span><span class="p">)</span>
	<span class="p">{</span> <span class="o">::</span><span class="k">new</span> <span class="p">(</span><span class="n">__functor</span><span class="p">.</span><span class="n">_M_access</span><span class="p">())</span> <span class="n">_Functor</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">__f</span><span class="p">));</span> <span class="p">}</span>

	<span class="k">static</span> <span class="kt">void</span>
	<span class="nf">_M_init_functor</span><span class="p">(</span><span class="n">_Any_data</span><span class="o">&amp;</span> <span class="n">__functor</span><span class="p">,</span> <span class="n">_Functor</span><span class="o">&amp;&amp;</span> <span class="n">__f</span><span class="p">,</span> <span class="n">false_type</span><span class="p">)</span>
	<span class="p">{</span> <span class="n">__functor</span><span class="p">.</span><span class="n">_M_access</span><span class="o">&lt;</span><span class="n">_Functor</span><span class="o">*&gt;</span><span class="p">()</span> <span class="o">=</span> <span class="k">new</span> <span class="n">_Functor</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">__f</span><span class="p">));</span> <span class="p">}</span>

</code></pre></div></div>

<h2 id="lamda-expression">lamda expression</h2>

<p><a href="https://en.cppreference.com/w/cpp/language/lambda">闭包</a>也是一个函数对象，类似<code class="language-plaintext highlighter-rouge">std::bind</code>。当lamda表达式以捕获值的形式捕获对象时，lamda表达式的结果的拷贝和移动构造函数由捕获的对象决定，因为捕获的对象将作为匿名闭包类型的成员：</p>

<blockquote>
  <p>The copy constructor and the move constructor are declared as defaulted and may be implicitly-defined according to the usual rules for copy constructors and move constructors.</p>
</blockquote>

<h1 id="可调用对象callable">可调用对象（Callable）</h1>

<p>所以，C++中可调用对象可以是如下类型：</p>

<ul>
  <li>函数类型（包括函数指针，函数类型会默认转换为函数指针）</li>
  <li>类成员函数指针</li>
  <li>类成员指针</li>
  <li>函数对象：任意实现了<code class="language-plaintext highlighter-rouge">()</code>操作符的类型，这一类包括
    <ul>
      <li><code class="language-plaintext highlighter-rouge">std::bind</code></li>
      <li><code class="language-plaintext highlighter-rouge">std::function</code></li>
      <li>lamda表达式</li>
    </ul>
  </li>
</ul>

<h2 id="callable就是能被stdinvoke调用的对象">Callable就是能被std::invoke调用的对象</h2>

<p>在C++ 17及以后，<code class="language-plaintext highlighter-rouge">std::invoke</code>可以直接调用<code class="language-plaintext highlighter-rouge">Callable</code>对象，其会根据传入的<code class="language-plaintext highlighter-rouge">Callable</code>类型，自动匹配转换，最终将参数传入函数调用执行。</p>]]></content><author><name></name></author><category term="C++" /><summary type="html"><![CDATA[本文试图在作者的知识范围内解释清楚C++中Callable这个概念。通常Callable总是跟函数直接等价，但是同时也包括其他可调用对象，例如std::function、std::bind、lamda表达式、成员函数、可调用对象(实现()操作符的类)等。以下内容函数特指全局定义的函数。]]></summary></entry><entry><title type="html">pimpl设计模式unique_ptr incomplete type问题</title><link href="http://localhost:4000/2023/12/24/pimpl%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8Funique_ptr-incomplete-type-%E9%97%AE%E9%A2%98.html" rel="alternate" type="text/html" title="pimpl设计模式unique_ptr incomplete type问题" /><published>2023-12-24T19:22:46+08:00</published><updated>2023-12-24T19:22:46+08:00</updated><id>http://localhost:4000/2023/12/24/pimpl%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8Funique_ptr-incomplete-type-%E9%97%AE%E9%A2%98</id><content type="html" xml:base="http://localhost:4000/2023/12/24/pimpl%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8Funique_ptr-incomplete-type-%E9%97%AE%E9%A2%98.html"><![CDATA[<p>本文不是介绍pimpl设计模式，而是关于在使用<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>实现pimpl设计模式时出现的编译问题及原因，以及与<code class="language-plaintext highlighter-rouge">std::shared_ptr</code>实现pimpl的区别。</p>

<p>相关文章：</p>

<ol>
  <li><a href="https://shan-weiqiang.github.io/2024/04/20/smartPtr-%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B.html">smartPtr内存模型</a></li>
  <li><a href="https://shan-weiqiang.github.io/2024/04/21/smartPtr-%E6%9E%84%E9%80%A0-%E6%9E%90%E6%9E%84%E8%A1%8C%E4%B8%BA%E5%8C%BA%E5%88%AB.html">smartPtr构造&amp;析构行为
</a></li>
</ol>

<ul id="markdown-toc">
  <li><a href="#问题描述" id="markdown-toc-问题描述">问题描述</a>    <ul>
      <li><a href="#someclassh" id="markdown-toc-someclassh"><code class="language-plaintext highlighter-rouge">someclass.h</code></a></li>
      <li><a href="#someclasscpp" id="markdown-toc-someclasscpp"><code class="language-plaintext highlighter-rouge">someclass.cpp</code></a></li>
      <li><a href="#appcpp" id="markdown-toc-appcpp"><code class="language-plaintext highlighter-rouge">app.cpp</code></a></li>
    </ul>
  </li>
  <li><a href="#问题原因" id="markdown-toc-问题原因">问题原因</a>    <ul>
      <li><a href="#stdunique_ptr源码分析" id="markdown-toc-stdunique_ptr源码分析"><code class="language-plaintext highlighter-rouge">std::unique_ptr</code>源码分析</a></li>
    </ul>
  </li>
  <li><a href="#解决办法" id="markdown-toc-解决办法">解决办法</a>    <ul>
      <li><a href="#为什么构造函数也要跟随析构函数一起在cpp中实现" id="markdown-toc-为什么构造函数也要跟随析构函数一起在cpp中实现">为什么构造函数也要跟随析构函数一起在cpp中实现</a></li>
      <li><a href="#为什么析构函数可以放在someclassimp定义的前面" id="markdown-toc-为什么析构函数可以放在someclassimp定义的前面">为什么析构函数可以放在SomeClassImp定义的前面</a></li>
    </ul>
  </li>
  <li><a href="#用stdunique_ptr而不是stdshared_ptr" id="markdown-toc-用stdunique_ptr而不是stdshared_ptr">用<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>而不是<code class="language-plaintext highlighter-rouge">std::shared_ptr</code></a></li>
  <li><a href="#最佳实践" id="markdown-toc-最佳实践">最佳实践</a></li>
</ul>

<h1 id="问题描述">问题描述</h1>

<h2 id="someclassh"><code class="language-plaintext highlighter-rouge">someclass.h</code></h2>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
</span>
<span class="k">class</span> <span class="nc">SomeClass</span>
<span class="p">{</span>
<span class="nl">public:</span>
	<span class="kt">void</span> <span class="n">do_some_thing</span><span class="p">();</span>

<span class="nl">private:</span>
	<span class="k">class</span> <span class="nc">SomeClassImp</span><span class="p">;</span>
	<span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">SomeClassImp</span><span class="o">&gt;</span> <span class="n">ptr</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<h2 id="someclasscpp"><code class="language-plaintext highlighter-rouge">someclass.cpp</code></h2>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"someclass.h"</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="k">class</span> <span class="nc">SomeClass</span><span class="o">::</span><span class="n">SomeClassImp</span>
<span class="p">{</span>
<span class="nl">public:</span>
	<span class="kt">void</span> <span class="n">implementation</span><span class="p">()</span>
	<span class="p">{</span>
		<span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"implementing...</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
	<span class="p">}</span>
<span class="p">};</span>
<span class="kt">void</span> <span class="n">SomeClass</span><span class="o">::</span><span class="n">do_some_thing</span><span class="p">()</span>
<span class="p">{</span>
	<span class="n">ptr</span><span class="o">-&gt;</span><span class="n">implementation</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="appcpp"><code class="language-plaintext highlighter-rouge">app.cpp</code></h2>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"someclass.h"</span><span class="cp">
</span><span class="kt">int</span> <span class="nf">main</span><span class="p">()</span>
<span class="p">{</span>
	<span class="n">SomeClass</span> <span class="n">some</span><span class="p">;</span>
	<span class="n">some</span><span class="p">.</span><span class="n">do_some_thing</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>

<p>编译以上代码编译器出现如下报错（gcc13):</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">/</span><span class="n">usr</span><span class="o">/</span><span class="n">local</span><span class="o">/</span><span class="n">include</span><span class="o">/</span><span class="n">c</span><span class="o">++/</span><span class="mf">9.5.0</span><span class="o">/</span><span class="n">bits</span><span class="o">/</span><span class="n">unique_ptr</span><span class="p">.</span><span class="n">h</span><span class="o">:</span><span class="mi">79</span><span class="o">:</span><span class="mi">16</span><span class="o">:</span> <span class="n">error</span><span class="o">:</span> <span class="n">invalid</span> <span class="n">application</span> <span class="n">of</span> <span class="err">'</span><span class="k">sizeof</span><span class="err">'</span> <span class="n">to</span> <span class="n">incomplete</span> <span class="n">type</span> <span class="err">'</span><span class="n">SomeClass</span><span class="o">::</span><span class="n">SomeClassImp</span><span class="err">'</span>
   <span class="mi">79</span> <span class="o">|</span>  <span class="k">static_assert</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="n">_Tp</span><span class="p">)</span><span class="o">&gt;</span><span class="mi">0</span><span class="p">,</span>
      <span class="o">|</span>                <span class="o">^~~~~~~~~~~</span>
</code></pre></div></div>

<h1 id="问题原因">问题原因</h1>

<p>直接原因是在编译编译单元<code class="language-plaintext highlighter-rouge">app.cpp</code>时，因为用户没有自定义析构函数，所以编译器会自动在<code class="language-plaintext highlighter-rouge">app.cpp</code>编译单元生成默认析构函数，而在析构成员<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>时报错，因为<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>析构函数需要知道模板参数类型的类型，而不能是incomplete type，而此时在<code class="language-plaintext highlighter-rouge">app.cpp</code>编译单元，<code class="language-plaintext highlighter-rouge">SomeClassImp</code>为incomplete type，所以报错。</p>

<h2 id="stdunique_ptr源码分析"><code class="language-plaintext highlighter-rouge">std::unique_ptr</code>源码分析</h2>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span> <span class="o">&lt;</span><span class="k">typename</span> <span class="nc">_Tp</span><span class="p">,</span> <span class="k">typename</span> <span class="nc">_Dp</span> <span class="o">=</span> <span class="n">default_delete</span><span class="o">&lt;</span><span class="n">_Tp</span><span class="p">&gt;</span><span class="o">&gt;</span>
    <span class="k">class</span> <span class="nc">unique_ptr</span>
    <span class="p">{</span>
<span class="p">...</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">std::unique_ptr</code>有两个模板参数，一个是指针指向的对象类型；另一个是该类型的<code class="language-plaintext highlighter-rouge">deleter</code>函数；如果用户不指定，则使用标准库默认的<code class="language-plaintext highlighter-rouge">default_delete&lt;_Tp&gt;</code>:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="nc">_Tp</span><span class="p">&gt;</span>
    <span class="k">struct</span> <span class="nc">default_delete</span>
    <span class="p">{</span>
      <span class="c1">/// Default constructor</span>
      <span class="k">constexpr</span> <span class="n">default_delete</span><span class="p">()</span> <span class="k">noexcept</span> <span class="o">=</span> <span class="k">default</span><span class="p">;</span>

      <span class="cm">/** @brief Converting constructor.
       *
       * Allows conversion from a deleter for arrays of another type, @p _Up,
       * only if @p _Up* is convertible to @p _Tp*.
       */</span>
      <span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="nc">_Up</span><span class="p">,</span> <span class="k">typename</span> <span class="o">=</span> <span class="k">typename</span>
	       <span class="nc">enable_if</span><span class="o">&lt;</span><span class="n">is_convertible</span><span class="o">&lt;</span><span class="n">_Up</span><span class="o">*</span><span class="p">,</span> <span class="n">_Tp</span><span class="o">*</span><span class="p">&gt;</span><span class="o">::</span><span class="n">value</span><span class="o">&gt;::</span><span class="n">type</span><span class="o">&gt;</span>
        <span class="n">default_delete</span><span class="p">(</span><span class="k">const</span> <span class="n">default_delete</span><span class="o">&lt;</span><span class="n">_Up</span><span class="o">&gt;&amp;</span><span class="p">)</span> <span class="k">noexcept</span> <span class="p">{</span> <span class="p">}</span>

      <span class="c1">/// Calls @c delete @p __ptr</span>
      <span class="kt">void</span>
      <span class="nf">operator</span><span class="p">()(</span><span class="n">_Tp</span><span class="o">*</span> <span class="n">__ptr</span><span class="p">)</span> <span class="k">const</span>
      <span class="p">{</span>
	<span class="k">static_assert</span><span class="p">(</span><span class="o">!</span><span class="n">is_void</span><span class="o">&lt;</span><span class="n">_Tp</span><span class="o">&gt;::</span><span class="n">value</span><span class="p">,</span>
		      <span class="s">"can't delete pointer to incomplete type"</span><span class="p">);</span>
	<span class="k">static_assert</span><span class="p">(</span><span class="k">sizeof</span><span class="p">(</span><span class="n">_Tp</span><span class="p">)</span><span class="o">&gt;</span><span class="mi">0</span><span class="p">,</span>
		      <span class="s">"can't delete pointer to incomplete type"</span><span class="p">);</span>
	<span class="k">delete</span> <span class="n">__ptr</span><span class="p">;</span>
      <span class="p">}</span>
    <span class="p">};</span>
</code></pre></div></div>

<p>可以看到在该<code class="language-plaintext highlighter-rouge">deleter</code>会被调用，且会判断是否是incomplete type： <code class="language-plaintext highlighter-rouge">static_assert(sizeof(*Tp)&gt;0</code>，*  在<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>析构函数中会调用这个<code class="language-plaintext highlighter-rouge">deleter</code>：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">~</span><span class="n">unique_ptr</span><span class="p">()</span> <span class="k">noexcept</span>
      <span class="p">{</span>
	<span class="k">static_assert</span><span class="p">(</span><span class="n">__is_invocable</span><span class="o">&lt;</span><span class="n">deleter_type</span><span class="o">&amp;</span><span class="p">,</span> <span class="n">pointer</span><span class="o">&gt;::</span><span class="n">value</span><span class="p">,</span>
		      <span class="s">"unique_ptr's deleter must be invocable with a pointer"</span><span class="p">);</span>
	<span class="k">auto</span><span class="o">&amp;</span> <span class="n">__ptr</span> <span class="o">=</span> <span class="n">_M_t</span><span class="p">.</span><span class="n">_M_ptr</span><span class="p">();</span>
	<span class="k">if</span> <span class="p">(</span><span class="n">__ptr</span> <span class="o">!=</span> <span class="nb">nullptr</span><span class="p">)</span>
	  <span class="n">get_deleter</span><span class="p">()(</span><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">__ptr</span><span class="p">));</span> <span class="c1">// 调用deleter</span>
	<span class="n">__ptr</span> <span class="o">=</span> <span class="n">pointer</span><span class="p">();</span>
      <span class="p">}</span>
</code></pre></div></div>

<p>所以，在使用<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>时，如果它的析构函数被编译，但是指向的类型仍然是incomplete type时，就会报错。</p>

<h1 id="解决办法">解决办法</h1>

<p>解决办法很简单，就是让编译器在知晓<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>指向的类型的具体定义后再生成<code class="language-plaintext highlighter-rouge">SomeClass</code>的析构函数，即将起析构函数的实现放在<code class="language-plaintext highlighter-rouge">someclass.cpp</code>，而不是在.<code class="language-plaintext highlighter-rouge">someclass.h</code>中默认实现：</p>

<p><code class="language-plaintext highlighter-rouge">someclass.h</code></p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
#include</span> <span class="cpf">&lt;memory&gt;</span><span class="cp">
</span>
<span class="k">class</span> <span class="nc">SomeClass</span>
<span class="p">{</span>
<span class="nl">public:</span>
	<span class="n">SomeClass</span><span class="p">();</span>
	<span class="o">~</span><span class="n">SomeClass</span><span class="p">();</span>
	<span class="kt">void</span> <span class="n">do_some_thing</span><span class="p">();</span>

<span class="nl">private:</span>
	<span class="k">class</span> <span class="nc">SomeClassImp</span><span class="p">;</span>
	<span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">SomeClassImp</span><span class="o">&gt;</span> <span class="n">ptr</span><span class="p">;</span>
<span class="p">};</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">someclass.cpp</code></p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">"someclass.h"</span><span class="cp">
#include</span> <span class="cpf">&lt;iostream&gt;</span><span class="cp">
</span>
<span class="n">SomeClass</span><span class="o">::</span><span class="n">SomeClass</span><span class="p">(){}</span>
<span class="n">SomeClass</span><span class="o">::~</span><span class="n">SomeClass</span><span class="p">(){}</span>

<span class="k">class</span> <span class="nc">SomeClass</span><span class="o">::</span><span class="n">SomeClassImp</span>
<span class="p">{</span>
<span class="nl">public:</span>
	<span class="kt">void</span> <span class="n">implementation</span><span class="p">()</span>
	<span class="p">{</span>
		<span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="s">"implementing...</span><span class="se">\n</span><span class="s">"</span><span class="p">;</span>
	<span class="p">}</span>
<span class="p">};</span>
<span class="kt">void</span> <span class="n">SomeClass</span><span class="o">::</span><span class="n">do_some_thing</span><span class="p">()</span>
<span class="p">{</span>
	<span class="n">ptr</span><span class="o">-&gt;</span><span class="n">implementation</span><span class="p">();</span>
<span class="p">}</span>
</code></pre></div></div>

<p>以上代码可以解决问题，但没有完全回答所有问题。</p>

<h2 id="为什么构造函数也要跟随析构函数一起在cpp中实现">为什么构造函数也要跟随析构函数一起在cpp中实现</h2>

<p>如上解决方案中如果仅仅在cpp文件中实现析构函数，而没有将构造函数一起实现，则会报同样的错误，问题的原因在于在构造函数时如果发生异常，编译器需要知道析构函数来讲对象析构，而此时编译器仍然会自动产生析构函数，问题跟之前一样</p>

<h2 id="为什么析构函数可以放在someclassimp定义的前面">为什么析构函数可以放在SomeClassImp定义的前面</h2>

<p>因为<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>是模板，根据模板的<a href="https://shan-weiqiang.github.io/2023/05/01/C++-template-name-lookup.html">二次查找规则</a>，当其析构函数被实例化时，整个编译单元的定义信息已经知道，所以即便<code class="language-plaintext highlighter-rouge">SomeClassImp</code>定义在<code class="language-plaintext highlighter-rouge">SomeClass</code>析构的后面，仍然能够正常编译。</p>

<p>另外参见Stackoverflow上的问题：<a href="https://stackoverflow.com/questions/77709516/in-pimpl-design-using-stdunique-ptr-if-dtor-is-put-in-implementation-file-bef">In pimpl design using std::unique_ptr, if dtor is put in implementation file BEFORE Impl type definition, why is it compiling ok?</a></p>

<h1 id="用stdunique_ptr而不是stdshared_ptr">用<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>而不是<code class="language-plaintext highlighter-rouge">std::shared_ptr</code></h1>

<p>如果使用<code class="language-plaintext highlighter-rouge">std::shared_ptr</code>，而不是<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>来实现pimpl，以上遇到的所有问题都会消失，因为<code class="language-plaintext highlighter-rouge">std::shared_ptr</code>并不会将所指向对象的<code class="language-plaintext highlighter-rouge">deleter</code>：</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="nc">_Tp</span><span class="p">&gt;</span>
    <span class="k">class</span> <span class="nc">shared_ptr</span> <span class="o">:</span> <span class="k">public</span> <span class="n">__shared_ptr</span><span class="o">&lt;</span><span class="n">_Tp</span><span class="o">&gt;</span>
    <span class="p">{</span>
<span class="p">...</span>
</code></pre></div></div>

<p>那为什么不使用<code class="language-plaintext highlighter-rouge">std::shared_ptr</code>，而是使用<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>呢？ 有如下的原因：</p>

<ul>
  <li>pimpl的设计默认定义了模糊指针指向的对象应该唯一的属于当前对象，<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>完美实现了这一点</li>
  <li><code class="language-plaintext highlighter-rouge">std::unique_ptr</code>有更好的运行时性能：不需要control block，没有引用计数等原子操作</li>
</ul>

<h1 id="最佳实践">最佳实践</h1>

<ul>
  <li>对于包含<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>的pimpl类，在cpp文件中定义析构函数和构造函数</li>
  <li>使用<code class="language-plaintext highlighter-rouge">std::unique_ptr</code>，而不是<code class="language-plaintext highlighter-rouge">std::shared_ptr</code></li>
</ul>]]></content><author><name></name></author><category term="C++" /><summary type="html"><![CDATA[本文不是介绍pimpl设计模式，而是关于在使用std::unique_ptr实现pimpl设计模式时出现的编译问题及原因，以及与std::shared_ptr实现pimpl的区别。]]></summary></entry></feed>